<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="aqua_food_processing.DashboardMain">
        <div class="o_aqua_dashboard">
            <div class="aqua-main">

                <!-- ══════════════════════ TOPBAR ══════════════════════ -->
                <div class="topbar">
                    <div>
                        <h1 t-esc="tabTitle"/>
                        <p t-esc="tabSubtitle"/>
                    </div>
                    <div class="top-actions">
                        <FilterBar onFilterChange.bind="onFilterChange" onRefresh.bind="onRefresh" isLoading="state.isLoading"/>
                        <div class="pill-input search-pill" style="position:relative;">
                            <i class="fa fa-search"/>
                            <input type="text" placeholder="Search receipts, orders, checks…"
                                   t-att-value="search.query"
                                   t-on-input="onSearchInput"
                                   t-on-focus="onSearchFocus"
                                   t-on-blur="onSearchBlur"/>
                            <i t-if="search.query" class="fa fa-times-circle search-clear" t-on-click="onSearchClear" title="Clear"/>
                            <div class="search-dropdown" t-if="search.isOpen">
                                <div t-if="search.loading" class="search-dropdown-msg">Searching…</div>
                                <div t-if="!search.loading and !search.results.length" class="search-dropdown-msg">No matches for "<t t-esc="search.query"/>"</div>
                                <t t-if="!search.loading" t-foreach="searchGroups" t-as="grp" t-key="grp.group">
                                    <div class="search-dropdown-group" t-esc="grp.group"/>
                                    <t t-foreach="grp.items" t-as="res" t-key="res.model + '-' + res.id">
                                        <div class="search-dropdown-row" t-on-mousedown="() => this.onSearchResultClick(res)">
                                            <span class="label" t-esc="res.label"/>
                                            <span class="sublabel" t-if="res.sublabel" t-esc="res.sublabel"/>
                                        </div>
                                    </t>
                                </t>
                            </div>
                        </div>
                        <div class="icon-btn" t-on-click="onRefresh" title="Refresh">
                            <i class="fa fa-bell"/><span class="badge-dot"/>
                        </div>
                        <button type="button" class="export-btn"><i class="fa fa-download"/>Export</button>
                    </div>
                </div>

                <!-- ══════════════════════ TAB BAR ══════════════════════ -->
                <div class="tabbar">
                    <button type="button" class="tab" t-att-class="{active: isTabActive('overview')}" t-on-click="() => this.setActiveTab('overview')">Overview</button>
                    <button type="button" class="tab" t-att-class="{active: isTabActive('procurement')}" t-on-click="() => this.setActiveTab('procurement')">Procurement</button>
                    <button type="button" class="tab" t-att-class="{active: isTabActive('processing')}" t-on-click="() => this.setActiveTab('processing')">Processing</button>
                    <button type="button" class="tab" t-att-class="{active: isTabActive('quality')}" t-on-click="() => this.setActiveTab('quality')">Quality Control</button>
                    <button type="button" class="tab" t-att-class="{active: isTabActive('budget')}" t-on-click="() => this.setActiveTab('budget')">Budget</button>
                </div>

                <!-- ══════════════════════ OVERVIEW TAB ══════════════════════ -->
                <t t-if="isTabActive('overview')">

                    <div class="grid g-hero">
                        <div class="aqua-card hero-card">
                            <h3>Processing overview</h3>
                            <p class="muted">Live overview of processing plant</p>
                            <div class="mini-stats">
                                <div class="mini-stat" style="background:var(--blue-tint);" t-on-click="onDrillTotalWeightReceived">
                                    <div class="val"><t t-esc="fmtTon(state.total_weight_received)"/></div>
                                    <div class="lbl">Raw material received</div>
                                </div>
                                <div class="mini-stat" style="background:var(--teal-tint);" t-on-click="onDrillTotalProcessingOrders">
                                    <div class="val"><t t-esc="fmtTon(state.total_input_qty)"/></div>
                                    <div class="lbl">In production</div>
                                </div>
                                <div class="mini-stat" style="background:var(--amber-tint);" t-on-click="onDrillTotalStockOnHand">
                                    <div class="val"><t t-esc="fmtTon(state.total_stock_on_hand)"/></div>
                                    <div class="lbl">Finished goods</div>
                                </div>
                                <div class="mini-stat" style="background:var(--purple-tint);">
                                    <div class="val"><t t-esc="fmtPct(ovYieldPct)"/></div>
                                    <div class="lbl">Yield (period)</div>
                                </div>
                            </div>
                            <div class="hero-img">
                                <img src="/aqua_food_processing/static/src/img/hero-processing.jpg" alt="Shrimp processing line"/>
                                <button type="button" class="hero-expand-btn" t-on-click="onHeroExpandClick" title="Expand">
                                    <i class="fa fa-expand"/>
                                </button>
                                <div class="hero-tag"><span class="dot"/>
                                    <t t-esc="state.active_blast_freeze_count"/> blast freeze cycle(s) active
                                </div>
                            </div>
                        </div>

                        <t t-if="ui.heroExpanded">
                            <div class="aqua-hero-lightbox" t-on-click="onHeroLightboxClose">
                                <button type="button" class="aqua-hero-lightbox-close" t-on-click="onHeroLightboxClose" title="Close">
                                    <i class="fa fa-times"/>
                                </button>
                                <img src="/aqua_food_processing/static/src/img/hero-processing.jpg" alt="Shrimp processing line" t-on-click="(ev) => ev.stopPropagation()"/>
                            </div>
                        </t>

                        <div class="aqua-card">
                            <div class="weather-top"><i class="fa fa-map-marker"/> <t t-esc="weather.locationLabel"/></div>

                            <t t-if="weather.loading">
                                <p class="muted" style="margin-top:8px;">Fetching current weather…</p>
                            </t>

                            <t t-if="!weather.loading and weather.error">
                                <div class="temp">--°</div>
                                <p class="muted">Weather unavailable right now.</p>
                            </t>

                            <t t-if="!weather.loading and !weather.error">
                                <div class="weather-row">
                                    <div class="temp"><t t-esc="weatherTempDisplay"/>°</div>
                                    <svg class="weather-icon" width="56" height="56" viewBox="0 0 64 64">
                                        <t t-if="weatherIconKey === 'sunny'">
                                            <g stroke="var(--amber)" stroke-width="3" stroke-linecap="round">
                                                <line x1="32" y1="4" x2="32" y2="12"/>
                                                <line x1="32" y1="52" x2="32" y2="60"/>
                                                <line x1="4" y1="32" x2="12" y2="32"/>
                                                <line x1="52" y1="32" x2="60" y2="32"/>
                                                <line x1="12.7" y1="12.7" x2="18.3" y2="18.3"/>
                                                <line x1="45.7" y1="45.7" x2="51.3" y2="51.3"/>
                                                <line x1="12.7" y1="51.3" x2="18.3" y2="45.7"/>
                                                <line x1="45.7" y1="18.3" x2="51.3" y2="12.7"/>
                                            </g>
                                            <circle cx="32" cy="32" r="14" fill="var(--amber)"/>
                                        </t>
                                        <t t-if="weatherIconKey === 'clear_night'">
                                            <path d="M40,8 A20,20 0 1 0 40,56 A24,24 0 0 1 40,8 Z" fill="var(--purple)"/>
                                        </t>
                                        <t t-if="weatherIconKey === 'partly_cloudy'">
                                            <g stroke="var(--amber)" stroke-width="3" stroke-linecap="round">
                                                <line x1="24" y1="4" x2="24" y2="9"/>
                                                <line x1="4" y1="24" x2="9" y2="24"/>
                                                <line x1="9.5" y1="9.5" x2="13" y2="13"/>
                                            </g>
                                            <circle cx="24" cy="24" r="11" fill="var(--amber)"/>
                                            <path d="M18,52 a11,11 0 0 1 -2,-21.8 a15,15 0 0 1 29,-4 a12,12 0 0 1 -2,25.8 Z" fill="#fff" stroke="#D9DEE4" stroke-width="1.5"/>
                                        </t>
                                        <t t-if="weatherIconKey === 'cloudy' or weatherIconKey === 'fog'">
                                            <path d="M16,50 a12,12 0 0 1 -2,-23.8 a16,16 0 0 1 31,-4.4 a13,13 0 0 1 -2,28.2 Z" fill="#CBD5E0"/>
                                            <g t-if="weatherIconKey === 'fog'" stroke="var(--ink-mute)" stroke-width="2.5" stroke-linecap="round">
                                                <line x1="14" y1="56" x2="50" y2="56"/>
                                                <line x1="18" y1="61" x2="46" y2="61"/>
                                            </g>
                                        </t>
                                        <t t-if="weatherIconKey === 'rain'">
                                            <path d="M16,42 a12,12 0 0 1 -2,-23.8 a16,16 0 0 1 31,-4.4 a13,13 0 0 1 -2,28.2 Z" fill="#9AA9C9"/>
                                            <g stroke="var(--blue)" stroke-width="3" stroke-linecap="round">
                                                <line x1="20" y1="50" x2="16" y2="60"/>
                                                <line x1="32" y1="50" x2="28" y2="60"/>
                                                <line x1="44" y1="50" x2="40" y2="60"/>
                                            </g>
                                        </t>
                                        <t t-if="weatherIconKey === 'snow'">
                                            <path d="M16,42 a12,12 0 0 1 -2,-23.8 a16,16 0 0 1 31,-4.4 a13,13 0 0 1 -2,28.2 Z" fill="#B9C4D6"/>
                                            <g stroke="#fff" stroke-width="2.5" stroke-linecap="round">
                                                <line x1="20" y1="48" x2="20" y2="58"/>
                                                <line x1="15.5" y1="53" x2="24.5" y2="53"/>
                                                <line x1="32" y1="48" x2="32" y2="58"/>
                                                <line x1="27.5" y1="53" x2="36.5" y2="53"/>
                                                <line x1="44" y1="48" x2="44" y2="58"/>
                                                <line x1="39.5" y1="53" x2="48.5" y2="53"/>
                                            </g>
                                        </t>
                                        <t t-if="weatherIconKey === 'storm'">
                                            <path d="M16,38 a12,12 0 0 1 -2,-23.8 a16,16 0 0 1 31,-4.4 a13,13 0 0 1 -2,28.2 Z" fill="#7A889E"/>
                                            <path d="M34,44 L24,58 L31,58 L27,64 L40,48 L33,48 Z" fill="var(--amber)"/>
                                        </t>
                                    </svg>
                                </div>
                                <p class="muted">
                                    <t t-esc="weatherConditionLabel"/> · Humidity <t t-esc="weatherHumidityDisplay"/>% · Wind <t t-esc="weatherWindDisplay"/> km/h
                                </p>
                                <p class="muted weather-updated" t-if="weather.updatedAt">Updated <t t-esc="weather.updatedAt"/></p>
                            </t>

                            <div class="inset-card">
                                <h3>Resource monitoring</h3>
                                <div class="donut-wrap">
                                    <svg width="150" height="150" viewBox="0 0 150 150">
                                        <circle cx="75" cy="75" r="58" fill="none" stroke="#EEF0F3" stroke-width="20"/>
                                        <t t-foreach="ovResourceMonitoringSegments" t-as="seg" t-key="seg.label">
                                            <circle cx="75" cy="75" r="58" fill="none" t-attf-stroke="{{seg.color}}" stroke-width="20" stroke-linecap="round" style="cursor:pointer;"
                                                    t-attf-stroke-dasharray="{{seg.dasharray}}" t-attf-stroke-dashoffset="{{seg.dashoffset}}"
                                                    transform="rotate(-90 75 75)"
                                                    t-on-click="() => this.onResourceMonitoringChartClick({label: seg.label})"
                                                    t-on-mouseenter="(ev) => this.onSvgPointEnter(ev, seg.label + ': ' + this.fmtTon(seg.value))"
                                                    t-on-mouseleave="() => this.onSvgPointLeave()"/>
                                        </t>
                                        <text x="75" y="71" text-anchor="middle" font-size="12" fill="var(--ink-mute)" font-family="Inter">Total</text>
                                        <text x="75" y="90" text-anchor="middle" font-size="17" font-weight="700" fill="var(--ink)" font-family="Inter"><t t-esc="fmtTonCompact(ovResourceMonitoringTotal)"/></text>
                                    </svg>
                                </div>
                                <div class="legend">
                                    <t t-foreach="ovResourceMonitoringParts" t-as="part" t-key="part.label">
                                        <div t-att-class="'legend-row ' + seriesLegendClass('resource_monitoring', part.label)" t-on-click="() => this.toggleSeries('resource_monitoring', part.label)" style="cursor:pointer;">
                                            <div class="lk"><span class="legend-dot" t-attf-style="background:{{part.color}};"/><t t-esc="part.label"/></div>
                                            <t t-esc="fmtPct(ovResourceMonitoringTotal ? (part.value / ovResourceMonitoringTotal * 100) : 0)"/>
                                        </div>
                                    </t>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="grid g-stats">
                        <div class="aqua-card stat-card" t-on-click="onDrillTotalProcessingOrders">
                            <div class="stat-top">
                                <div class="stat-icon" style="background:var(--blue-tint); color:var(--blue);"><i class="fa fa-bar-chart"/></div>
                                <t t-set="chip" t-value="comparisonChip('total_input_qty')"/>
                                <span t-if="chip" class="chip" t-attf-style="background:{{chip.bg}}; color:{{chip.color}};"><t t-esc="chip.text"/></span>
                            </div>
                            <div><div class="stat-val"><t t-esc="fmtTon(state.total_input_qty)"/></div><div class="stat-lbl">Input this period</div></div>
                        </div>
                        <div class="aqua-card stat-card">
                            <div class="stat-top">
                                <div class="stat-icon" style="background:var(--teal-tint); color:var(--teal);"><i class="fa fa-line-chart"/></div>
                            </div>
                            <div><div class="stat-val"><t t-esc="fmtPct(ovYieldPct)"/></div><div class="stat-lbl">Overall yield</div></div>
                        </div>
                        <div class="aqua-card stat-card" t-on-click="onDrillCancelledReceipts">
                            <div class="stat-top">
                                <div class="stat-icon" style="background:var(--coral-tint); color:var(--coral);"><i class="fa fa-exclamation-triangle"/></div>
                                <t t-set="chip" t-value="comparisonChip('rejection_rate', true)"/>
                                <span t-if="chip" class="chip" t-attf-style="background:{{chip.bg}}; color:{{chip.color}};"><t t-esc="chip.text"/></span>
                            </div>
                            <div><div class="stat-val"><t t-esc="fmtPct(state.rejection_rate)"/></div><div class="stat-lbl">Rejection rate</div></div>
                        </div>
                        <div class="aqua-card stat-card" t-on-click="onDrillTotalProcessingOrders">
                            <div class="stat-top">
                                <div class="stat-icon" style="background:var(--purple-tint); color:var(--purple);"><i class="fa fa-users"/></div>
                                <span class="chip" style="background:#F1F2F4; color:var(--ink-soft);"><t t-esc="state.active_vendor_count"/> vendors active</span>
                            </div>
                            <div><div class="stat-val"><t t-esc="procgOrderedByStatus.done"/> orders</div><div class="stat-lbl">Completed this period</div></div>
                        </div>
                    </div>

                    <div class="grid g-mid">
                        <div class="aqua-card">
                            <h3>Production progress</h3>
                            <div class="arc-wrap">
                                <svg width="140" height="90" viewBox="0 0 140 90">
                                    <path d="M10,80 A60,60 0 0 1 130,80" fill="none" stroke="#EEF0F3" stroke-width="14" stroke-linecap="round"/>
                                    <path d="M10,80 A60,60 0 0 1 130,80" fill="none" stroke="var(--blue)" stroke-width="14" stroke-linecap="round"
                                          t-att-stroke-dasharray="semiArcDash(ovYieldPct, 60)"/>
                                    <text x="70" y="65" text-anchor="middle" font-size="22" font-weight="700" fill="var(--ink)" font-family="Inter"><t t-esc="fmtPct(ovYieldPct)"/></text>
                                </svg>
                            </div>
                            <p class="muted" style="text-align:center;">
                                Finished goods <t t-esc="fmtTon(state.total_stock_on_hand)"/> / input <t t-esc="fmtTon(state.total_input_qty)"/>
                            </p>
                            <div style="margin-top:14px;">
                                <t t-foreach="qIpqcByOperationRows" t-as="op" t-key="op.label">
                                    <div class="line-row">
                                        <span class="lbl lbl-sm" t-esc="op.label"/>
                                        <div class="track"><div class="fill" t-attf-style="width:{{op.passPct}}%;"/></div>
                                        <span class="pct pct-sm"><t t-esc="op.passPct"/>%</span>
                                    </div>
                                </t>
                            </div>
                        </div>

                        <div class="aqua-card">
                            <h3>Quality control</h3>
                            <p class="muted"><t t-esc="state.qc_total"/> checks this period</p>
                            <div style="margin-top:10px;">
                                <t t-foreach="ovQcRows" t-as="row" t-key="row.label">
                                    <div class="qc-row" t-on-click="() => this.onQcChartClick({label: row.label})" style="cursor:pointer;">
                                        <span><span class="qc-dot" t-attf-style="background:{{row.color}};"/><t t-esc="row.label"/></span>
                                        <b t-esc="row.value"/>
                                    </div>
                                </t>
                            </div>
                            <h3 style="margin-top:18px; font-size:13px;">Checks by stage</h3>
                            <t t-foreach="state.qc_stage_breakdown" t-as="row" t-key="row.label">
                                <div class="qc-row" style="font-size:12.5px; cursor:pointer;" t-on-click="() => this.onQcStageChartClick({label: row.label})">
                                    <span t-esc="row.label"/><b t-esc="row.value"/>
                                </div>
                            </t>
                        </div>

                        <div class="aqua-card">
                            <h3>Cold storage status</h3>
                            <div class="arc-wrap" style="margin-top:14px;">
                                <svg width="130" height="130" viewBox="0 0 130 130">
                                    <circle cx="65" cy="65" r="52" fill="none" stroke="#EEF0F3" stroke-width="16"/>
                                    <circle cx="65" cy="65" r="52" fill="none" stroke="var(--blue)" stroke-width="16" stroke-linecap="round"
                                            t-att-stroke-dasharray="fullArcDash(ovColdRoomOverallPct, 52)"
                                            transform="rotate(-90 65 65)"/>
                                    <text x="65" y="71" text-anchor="middle" font-size="20" font-weight="700" fill="var(--ink)" font-family="Inter"><t t-esc="fmtPct(ovColdRoomOverallPct)"/></text>
                                </svg>
                            </div>
                            <p class="muted" style="text-align:center;">Weighted across all cold rooms</p>
                            <t t-foreach="state.cold_room_utilization" t-as="room" t-key="room.name">
                                <div class="storage-row" t-on-click="() => this.onColdRoomChartClick({label: room.name})" style="cursor:pointer;">
                                    <span t-esc="room.name"/>
                                    <b><t t-esc="fmtTon(room.capacity_kg * room.pct / 100)"/></b>
                                </div>
                            </t>
                        </div>
                    </div>

                    <div class="grid g-bottom">
                        <div class="aqua-card">
                            <h3>Recent activity — receipt trend</h3>
                            <p class="muted" t-esc="receiptTrendTitle"/>
                            <!-- No timestamped activity feed exists in the data model, so
                                 the receipt trend (which is real, period-scoped data)
                                 stands in for the mockup's "today's activity" timeline. -->
                            <svg width="100%" height="150" viewBox="0 0 580 160" style="margin-top:8px;" t-if="state.receipt_trend.length">
                                <t t-set="g" t-value="lineChartGeometry(state.receipt_trend, {height: 160})"/>
                                <t t-foreach="g.gridLines" t-as="gl" t-key="gl_index">
                                    <line t-att-x1="g.padL" t-att-y1="gl.y" t-att-x2="g.width - g.padR" t-att-y2="gl.y" stroke="var(--line)"/>
                                    <text t-att-x="g.padL - 10" t-att-y="gl.y + 4" text-anchor="end" font-size="10" fill="var(--ink-mute)" font-family="Inter" t-esc="gl.label"/>
                                </t>
                                <path t-att-d="g.areaPath" fill="var(--teal)" opacity="0.15"/>
                                <polyline t-att-points="g.polyline" fill="none" stroke="var(--teal)" stroke-width="2.5"/>
                                <t t-foreach="g.points" t-as="p" t-key="p_index">
                                    <circle t-att-cx="p.x" t-att-cy="p.y" r="3.5" fill="var(--teal)"/>
                                    <circle t-att-cx="p.x" t-att-cy="p.y" r="11" fill="transparent" style="cursor:pointer;"
                                            t-on-click="() => this.onReceiptTrendChartClick({label: p.label})"
                                            t-on-mouseenter="(ev) => this.onSvgPointEnter(ev, p.label + ': ' + this.fmtNum(p.value) + ' receipt(s)')"
                                            t-on-mouseleave="() => this.onSvgPointLeave()"/>
                                    <text t-att-x="p.x" t-att-y="g.baseline + 18" text-anchor="middle" font-size="10.5" fill="var(--ink-mute)" font-family="Inter" t-esc="p.label"/>
                                </t>
                            </svg>
                            <div class="o_aqua_empty" t-else="">No catch receipts recorded yet.</div>
                        </div>

                        <div class="aqua-card">
                            <div class="card-head-row">
                                <h3>Shipment status</h3>
                                <span class="view-all" t-on-click="() => this.setActiveTab('quality')">View all</span>
                            </div>
                            <p class="muted">By current status, this period</p>
                            <t t-foreach="ovShipmentRows" t-as="row" t-key="row.label">
                                <div class="ship-card" t-on-click="() => this.onShipmentChartClick({label: row.label})">
                                    <div class="ship-head">
                                        <b style="font-size:13.5px;" t-esc="row.label"/>
                                        <span style="font-size:12.5px; font-weight:600;"><t t-esc="row.value"/> shipment(s)</span>
                                    </div>
                                    <span class="status-pill" t-attf-style="background:{{row.color}}22; color:{{row.color}};" t-esc="row.label"/>
                                </div>
                            </t>
                            <div class="o_aqua_empty" t-if="!ovShipmentRows.length">No shipments recorded yet.</div>
                        </div>
                    </div>

                    <div class="grid" style="grid-template-columns:1.6fr 1fr; margin-top:18px;">
                        <div class="aqua-card">
                            <h3>Inventory at a glance</h3>
                            <div class="inv-grid">
                                <div class="inv-item" t-on-click="onDrillTotalWeightReceived">
                                    <div class="inv-icon" style="background:var(--blue-tint); color:var(--blue);"><i class="fa fa-shopping-basket"/></div>
                                    <div class="inv-val"><t t-esc="fmtTon(state.total_weight_received)"/></div>
                                    <div class="inv-lbl">Raw material</div>
                                </div>
                                <div class="inv-item" t-on-click="onDrillWipStock">
                                    <div class="inv-icon" style="background:var(--teal-tint); color:var(--teal);"><i class="fa fa-cog"/></div>
                                    <div class="inv-val"><t t-esc="fmtTon(state.wip_stock_kg)"/></div>
                                    <div class="inv-lbl">WIP</div>
                                </div>
                                <div class="inv-item" t-on-click="onDrillTotalStockOnHand">
                                    <div class="inv-icon" style="background:var(--purple-tint); color:var(--purple);"><i class="fa fa-snowflake-o"/></div>
                                    <div class="inv-val"><t t-esc="fmtTon(state.total_stock_on_hand)"/></div>
                                    <div class="inv-lbl">Stock on hand</div>
                                </div>
                                <div class="inv-item" t-on-click="onDrillActiveVendors">
                                    <div class="inv-icon" style="background:var(--amber-tint); color:var(--amber);"><i class="fa fa-truck"/></div>
                                    <div class="inv-val"><t t-esc="state.active_vendor_count"/></div>
                                    <div class="inv-lbl">Active vendors</div>
                                </div>
                                <div class="inv-item">
                                    <div class="inv-icon" style="background:var(--coral-tint); color:var(--coral);"><i class="fa fa-snowflake-o"/></div>
                                    <div class="inv-val"><t t-esc="state.active_blast_freeze_count"/></div>
                                    <div class="inv-lbl">Blast freeze cycles</div>
                                </div>
                            </div>
                        </div>

                        <div class="aqua-card">
                            <div class="card-head-row">
                                <h3>Attention needed</h3>
                                <span class="view-all" t-on-click="onDrillQcHold">View all</span>
                            </div>
                            <div style="margin-top:10px;">
                                <div class="alert-row" t-if="state.qc_hold_count" t-on-click="onDrillQcHold" style="cursor:pointer;">
                                    <div class="alert-icon"><i class="fa fa-exclamation-triangle"/></div>
                                    <div><div class="t-title" style="font-size:13px;"><t t-esc="state.qc_hold_count"/> quality check(s) on hold</div><div class="t-sub">Tap to review</div></div>
                                </div>
                                <div class="alert-row" t-if="state.qc_fail_count" t-on-click="onDrillQcFail" style="cursor:pointer;">
                                    <div class="alert-icon"><i class="fa fa-exclamation-triangle"/></div>
                                    <div><div class="t-title" style="font-size:13px;"><t t-esc="state.qc_fail_count"/> failed quality check(s)</div><div class="t-sub">Tap to review</div></div>
                                </div>
                                <div class="alert-row" t-if="state.cancelled_receipts" t-on-click="onDrillCancelledReceipts" style="cursor:pointer;">
                                    <div class="alert-icon"><i class="fa fa-exclamation-triangle"/></div>
                                    <div><div class="t-title" style="font-size:13px;"><t t-esc="state.cancelled_receipts"/> cancelled catch receipt(s)</div><div class="t-sub">Tap to review</div></div>
                                </div>
                                <div class="o_aqua_empty" t-if="!state.qc_hold_count and !state.qc_fail_count and !state.cancelled_receipts">Nothing needs attention right now.</div>
                            </div>
                        </div>
                    </div>
                </t>

                <!-- ══════════════════════ PROCUREMENT TAB ══════════════════════ -->
                <t t-if="isTabActive('procurement')">

                    <div class="grid g-kpi6">
                        <div class="aqua-card kpi-tile" t-on-click="onDrillTotalReceipts">
                            <div class="stat-top"><div class="stat-icon" style="background:var(--blue-tint); color:var(--blue);"><i class="fa fa-clipboard"/></div></div>
                            <div><div class="stat-val" t-esc="fmtNum(state.total_receipts)"/><div class="stat-lbl">Total receipts</div></div>
                        </div>
                        <div class="aqua-card kpi-tile" t-on-click="onDrillTotalWeightReceived">
                            <div class="stat-top"><div class="stat-icon" style="background:var(--teal-tint); color:var(--teal);"><i class="fa fa-balance-scale"/></div></div>
                            <div><div class="stat-val" t-esc="fmtTon(state.total_weight_received)"/><div class="stat-lbl">Weight received</div></div>
                        </div>
                        <div class="aqua-card kpi-tile" t-on-click="onDrillActiveVendors">
                            <div class="stat-top"><div class="stat-icon" style="background:var(--purple-tint); color:var(--purple);"><i class="fa fa-users"/></div></div>
                            <div><div class="stat-val" t-esc="fmtNum(state.active_vendor_count)"/><div class="stat-lbl">Active vendors</div></div>
                        </div>
                        <div class="aqua-card kpi-tile" t-on-click="onDrillTotalPurchaseSpend">
                            <div class="stat-top"><div class="stat-icon" style="background:var(--amber-tint); color:var(--amber);"><i class="fa fa-inr"/></div></div>
                            <div><div class="stat-val" t-esc="fmtINR(state.total_purchase_spend)"/><div class="stat-lbl">Purchase spend</div></div>
                        </div>
                        <div class="aqua-card kpi-tile" t-on-click="onDrillTotalPurchaseSpend">
                            <div class="stat-top"><div class="stat-icon" style="background:var(--green-tint); color:var(--green);"><i class="fa fa-money"/></div></div>
                            <div><div class="stat-val" t-esc="fmtINRPerKg(state.avg_price_per_kg)"/><div class="stat-lbl">Avg price / kg</div></div>
                        </div>
                        <div class="aqua-card kpi-tile" t-on-click="onDrillCancelledReceipts">
                            <div class="stat-top"><div class="stat-icon" style="background:var(--coral-tint); color:var(--coral);"><i class="fa fa-times"/></div></div>
                            <div><div class="stat-val" t-esc="fmtPct(state.rejection_rate)"/><div class="stat-lbl">Rejection rate</div></div>
                        </div>
                    </div>

                    <div class="section-sub" style="margin-top:0;">Live inventory (current stock)</div>
                    <div class="grid g-two">
                        <div class="aqua-card" style="display:flex; align-items:center; justify-content:space-between; cursor:pointer;" t-on-click="onDrillTotalStockOnHand">
                            <div style="display:flex; align-items:center; gap:16px;">
                                <div class="stat-icon" style="width:44px; height:44px; background:var(--green-tint); color:var(--green); font-size:20px;"><i class="fa fa-cubes"/></div>
                                <div><div class="stat-val" style="font-size:26px;" t-esc="fmtTon(state.total_stock_on_hand)"/><div class="stat-lbl">Current stock on hand</div></div>
                            </div>
                            <svg width="90" height="36" viewBox="0 0 90 36"><polyline points="0,26 20,22 40,24 60,12 90,6" fill="none" stroke="var(--green)" stroke-width="2.5"/></svg>
                        </div>
                        <div class="aqua-card" style="display:flex; align-items:center; justify-content:space-between; cursor:pointer;" t-on-click="onDrillStockValue">
                            <div style="display:flex; align-items:center; gap:16px;">
                                <div class="stat-icon" style="width:44px; height:44px; background:var(--blue-tint); color:var(--blue); font-size:20px;"><i class="fa fa-inr"/></div>
                                <div><div class="stat-val" style="font-size:26px;" t-esc="fmtINR(state.stock_value)"/><div class="stat-lbl">Stock value</div></div>
                            </div>
                            <svg width="90" height="36" viewBox="0 0 90 36"><polyline points="0,20 20,24 40,14 60,18 90,4" fill="none" stroke="var(--blue)" stroke-width="2.5"/></svg>
                        </div>
                    </div>

                    <div class="grid g-mid">
                        <div class="aqua-card">
                            <h3>Receipt status</h3>
                            <p class="muted">Open · Completed · Cancelled</p>
                            <div class="donut-wrap">
                                <svg width="150" height="150" viewBox="0 0 150 150">
                                    <circle cx="75" cy="75" r="58" fill="none" stroke="#EEF0F3" stroke-width="20"/>
                                    <t t-foreach="procReceiptStatusSegments" t-as="seg" t-key="seg.label">
                                        <circle cx="75" cy="75" r="58" fill="none" stroke-width="20" stroke-linecap="round" style="cursor:pointer;"
                                                t-att-stroke="seg.color" t-att-stroke-dasharray="seg.dasharray" t-att-stroke-dashoffset="seg.dashoffset"
                                                transform="rotate(-90 75 75)"
                                                t-on-click="() => this.onReceiptStatusChartClick({label: seg.label})"
                                                t-on-mouseenter="(ev) => this.onSvgPointEnter(ev, seg.label + ': ' + this.fmtNum(seg.value))"
                                                t-on-mouseleave="() => this.onSvgPointLeave()"/>
                                    </t>
                                    <text x="75" y="71" text-anchor="middle" font-size="12" fill="var(--ink-mute)" font-family="Inter">Total</text>
                                    <text x="75" y="90" text-anchor="middle" font-size="18" font-weight="700" fill="var(--ink)" font-family="Inter" t-esc="fmtNum(state.total_receipts)"/>
                                </svg>
                            </div>
                            <div class="legend">
                                <t t-foreach="ovReceiptStatusRows" t-as="row" t-key="row.label">
                                    <div t-att-class="'legend-row ' + seriesLegendClass('receipt_status', row.label)" t-on-click="() => this.toggleSeries('receipt_status', row.label)" style="cursor:pointer;">
                                        <div class="lk"><span class="legend-dot" t-attf-style="background:{{row.color}};"/><t t-esc="row.label"/></div>
                                        <t t-esc="row.value"/>
                                    </div>
                                </t>
                            </div>
                        </div>

                        <div class="aqua-card">
                            <h3>Receipts by species</h3>
                            <p class="muted">Count of catch receipts</p>
                            <div style="margin-top:4px;">
                                <t t-foreach="procSpeciesRows" t-as="row" t-key="row.label">
                                    <div class="line-row" t-on-click="() => this.onSpeciesChartClick({label: row.label})" style="cursor:pointer;">
                                        <span class="lbl" t-esc="row.label"/>
                                        <div class="track"><div class="fill" t-attf-style="width:{{barPct(row.value, procSpeciesRows)}}%; background:{{row.color}};"/></div>
                                        <span class="pct" t-esc="row.value"/>
                                    </div>
                                </t>
                            </div>
                        </div>

                        <div class="aqua-card">
                            <h3>Spend by vendor</h3>
                            <p class="muted">Top vendors, this period</p>
                            <div style="margin-top:4px;">
                                <t t-foreach="procSpendByVendorRows" t-as="row" t-key="row.label">
                                    <div class="line-row" t-on-click="() => this.onSpendByVendorChartClick({label: row.label})" style="cursor:pointer;">
                                        <span class="lbl" t-esc="row.label"/>
                                        <div class="track"><div class="fill" t-attf-style="width:{{barPct(row.value, procSpendByVendorRows)}}%;"/></div>
                                        <span class="pct" t-esc="fmtINR(row.value)"/>
                                    </div>
                                </t>
                            </div>
                        </div>
                    </div>

                    <div class="grid" style="grid-template-columns:1fr 1.2fr; margin-bottom:18px;">
                        <div class="aqua-card">
                            <h3>Purchase-to-stock funnel</h3>
                            <p class="muted">Ordered → received → in stock now</p>
                            <div class="funnel">
                                <t t-foreach="state.purchase_to_stock_funnel" t-as="row" t-key="row.label">
                                    <div class="funnel-row" t-on-click="() => this.onPurchaseToStockFunnelChartClick({label: row.label})" style="cursor:pointer;">
                                        <span class="funnel-lbl" t-esc="row.label"/>
                                        <div class="funnel-bar-wrap">
                                            <div class="funnel-bar" t-attf-style="width:{{barPct(row.value, state.purchase_to_stock_funnel)}}%; background:{{cyclePalette(row_index)}};">
                                                <t t-esc="fmtTon(row.value)"/>
                                            </div>
                                        </div>
                                    </div>
                                </t>
                            </div>
                        </div>

                        <div class="aqua-card">
                            <h3>Ordered vs received</h3>
                            <p class="muted">Most recent receipts with a purchase order</p>
                            <div class="bar-col-wrap">
                                <t t-foreach="procOrderedVsReceivedRows" t-as="row" t-key="row.label">
                                    <div class="bar-col" t-on-click="() => this.onOrderedVsReceivedChartClick({label: row.label})" style="cursor:pointer;"
                                         t-on-mouseenter="(ev) => this.onSvgPointEnter(ev, row.label + ' — Ordered: ' + this.fmtNum(row.ordered) + ' kg · Received: ' + this.fmtNum(row.received) + ' kg')"
                                         t-on-mouseleave="() => this.onSvgPointLeave()">
                                        <div class="v" t-esc="row.received"/>
                                        <div class="stick" t-if="isSeriesVisible('ordered_vs_received', 'ordered')" t-attf-style="height:{{row.orderedPct}}%;"/>
                                        <div class="stick b2" t-if="isSeriesVisible('ordered_vs_received', 'received')" t-attf-style="height:{{row.receivedPct}}%;"/>
                                        <div class="l" t-esc="row.label"/>
                                    </div>
                                </t>
                            </div>
                            <div class="bar-col-legend">
                                <div t-att-class="seriesLegendClass('ordered_vs_received', 'ordered')" style="cursor:pointer;" t-on-click.stop="() => this.toggleSeries('ordered_vs_received', 'ordered')"><span class="legend-dot" style="background:var(--blue); margin-right:6px;"/>Ordered (kg)</div>
                                <div t-att-class="seriesLegendClass('ordered_vs_received', 'received')" style="cursor:pointer;" t-on-click.stop="() => this.toggleSeries('ordered_vs_received', 'received')"><span class="legend-dot" style="background:var(--teal); margin-right:6px;"/>Received (kg)</div>
                            </div>
                        </div>
                    </div>

                    <div class="grid g-two">
                        <div class="aqua-card">
                            <h3>Weight received trend</h3>
                            <p class="muted" t-esc="'Weight received trend (' + state.trend_granularity_label + ', kg)'"/>
                            <svg width="100%" height="190" viewBox="0 0 580 200" style="margin-top:8px;" t-if="state.daily_weight_trend.length">
                                <t t-set="g" t-value="procWeightTrendGeom"/>
                                <t t-foreach="g.gridLines" t-as="gl" t-key="gl_index">
                                    <line t-att-x1="g.padL" t-att-y1="gl.y" t-att-x2="g.width - g.padR" t-att-y2="gl.y" stroke="var(--line)"/>
                                    <text t-att-x="g.padL - 10" t-att-y="gl.y + 4" text-anchor="end" font-size="10" fill="var(--ink-mute)" font-family="Inter" t-esc="gl.label"/>
                                </t>
                                <path t-att-d="g.areaPath" fill="var(--teal)" opacity="0.15"/>
                                <polyline t-att-points="g.polyline" fill="none" stroke="var(--teal)" stroke-width="2.5"/>
                                <t t-foreach="g.points" t-as="p" t-key="p_index">
                                    <circle t-att-cx="p.x" t-att-cy="p.y" r="3.5" fill="var(--teal)"/>
                                    <circle t-att-cx="p.x" t-att-cy="p.y" r="11" fill="transparent" style="cursor:pointer;"
                                            t-on-click="() => this.onDailyWeightTrendChartClick({label: p.label})"
                                            t-on-mouseenter="(ev) => this.onSvgPointEnter(ev, p.label + ': ' + this.fmtTon(p.value))"
                                            t-on-mouseleave="() => this.onSvgPointLeave()"/>
                                    <text t-att-x="p.x" t-att-y="g.baseline + 18" text-anchor="middle" font-size="10.5" fill="var(--ink-mute)" font-family="Inter" t-esc="p.label"/>
                                </t>
                            </svg>
                            <div class="o_aqua_empty" t-else="">Not enough data yet for a trend line.</div>
                        </div>

                        <div class="aqua-card">
                            <h3>Purchase spend trend</h3>
                            <p class="muted" t-esc="'Purchase spend trend (' + state.trend_granularity_label + ', ₹)'"/>
                            <svg width="100%" height="190" viewBox="0 0 580 200" style="margin-top:8px;" t-if="state.purchase_spend_trend.length">
                                <t t-set="g" t-value="procSpendTrendGeom"/>
                                <t t-foreach="g.gridLines" t-as="gl" t-key="gl_index">
                                    <line t-att-x1="g.padL" t-att-y1="gl.y" t-att-x2="g.width - g.padR" t-att-y2="gl.y" stroke="var(--line)"/>
                                    <text t-att-x="g.padL - 10" t-att-y="gl.y + 4" text-anchor="end" font-size="10" fill="var(--ink-mute)" font-family="Inter" t-esc="gl.label"/>
                                </t>
                                <path t-att-d="g.areaPath" fill="var(--purple)" opacity="0.15"/>
                                <polyline t-att-points="g.polyline" fill="none" stroke="var(--purple)" stroke-width="2.5"/>
                                <t t-foreach="g.points" t-as="p" t-key="p_index">
                                    <circle t-att-cx="p.x" t-att-cy="p.y" r="3.5" fill="var(--purple)"/>
                                    <circle t-att-cx="p.x" t-att-cy="p.y" r="11" fill="transparent" style="cursor:pointer;"
                                            t-on-click="() => this.onPurchaseSpendTrendChartClick({label: p.label})"
                                            t-on-mouseenter="(ev) => this.onSvgPointEnter(ev, p.label + ': ' + this.fmtINR(p.value))"
                                            t-on-mouseleave="() => this.onSvgPointLeave()"/>
                                    <text t-att-x="p.x" t-att-y="g.baseline + 18" text-anchor="middle" font-size="10.5" fill="var(--ink-mute)" font-family="Inter" t-esc="p.label"/>
                                </t>
                            </svg>
                            <div class="o_aqua_empty" t-else="">Not enough data yet for a trend line.</div>
                        </div>
                    </div>

                    <div class="grid" style="grid-template-columns:1fr; margin-bottom:18px;">
                        <div class="aqua-card">
                            <h3>Received weight by vendor</h3>
                            <p class="muted">Top vendors by weight actually received</p>
                            <div style="margin-top:4px;">
                                <t t-foreach="procWeightByVendorRows" t-as="row" t-key="row.label">
                                    <div class="line-row" t-on-click="() => this.onWeightByVendorChartClick({label: row.label})" style="cursor:pointer;">
                                        <span class="lbl" t-esc="row.label"/>
                                        <div class="track"><div class="fill" t-attf-style="width:{{barPct(row.value, procWeightByVendorRows)}}%; background:{{row.color}};"/></div>
                                        <span class="pct" t-esc="fmtTon(row.value)"/>
                                    </div>
                                </t>
                            </div>
                        </div>
                    </div>

                    <div class="grid g-bottom">
                        <div class="aqua-card">
                            <h3>Today's stock by product</h3>
                            <p class="muted">Live raw material on hand</p>
                            <div style="margin-top:4px;">
                                <t t-foreach="procStockByProductRows" t-as="row" t-key="row.label">
                                    <div class="line-row" t-on-click="() => this.onCurrentStockByProductChartClick({label: row.label})" style="cursor:pointer;">
                                        <span class="lbl" t-esc="row.label"/>
                                        <div class="track"><div class="fill" t-attf-style="width:{{barPct(row.value, procStockByProductRows)}}%; background:{{row.color}};"/></div>
                                        <span class="pct" t-esc="fmtTon(row.value)"/>
                                    </div>
                                </t>
                            </div>
                        </div>
                        <div class="aqua-card">
                            <h3>Today's stock by location</h3>
                            <p class="muted">Where raw material physically sits</p>
                            <div style="margin-top:4px;">
                                <t t-foreach="procStockByLocationRows" t-as="row" t-key="row.label">
                                    <div class="line-row" t-on-click="() => this.onCurrentStockByLocationChartClick({label: row.label})" style="cursor:pointer;">
                                        <span class="lbl" t-esc="row.label"/>
                                        <div class="track"><div class="fill" t-attf-style="width:{{barPct(row.value, procStockByLocationRows)}}%; background:{{row.color}};"/></div>
                                        <span class="pct" t-esc="fmtTon(row.value)"/>
                                    </div>
                                </t>
                            </div>
                        </div>
                    </div>

                    <div class="aqua-card">
                        <div class="card-head-row">
                            <h3>Recent catch receipts</h3>
                        </div>
                        <p class="muted">Purchase to storage — most recent deliveries</p>
                        <div class="table-scroll">
                        <table class="dtable" t-if="state.recent_receipts_table.length">
                            <thead><tr><th>Receipt</th><th>Vendor</th><th>PO</th><th>Species</th><th>Ordered</th><th>Received</th><th>Status</th></tr></thead>
                            <tbody>
                                <t t-foreach="state.recent_receipts_table" t-as="row" t-key="row.id">
                                    <tr t-on-click="() => this.onRecentReceiptRowClick(row.receipt_id)">
                                        <td t-esc="row.receipt"/>
                                        <td class="soft" t-esc="row.vendor"/>
                                        <td class="soft" t-esc="row.po"/>
                                        <td t-esc="row.species"/>
                                        <td class="soft" t-esc="row.ordered_qty"/>
                                        <td t-esc="row.quantity"/>
                                        <td><span t-att-class="'pill-sm ' + badgeClass(row.receipt_state)" t-esc="row.receipt_state"/></td>
                                    </tr>
                                </t>
                            </tbody>
                        </table>
                        </div>
                        <div class="o_aqua_empty" t-if="!state.recent_receipts_table.length">No catch receipts recorded yet.</div>
                    </div>
                </t>

                <!-- ══════════════════════ PROCESSING TAB ══════════════════════ -->
                <t t-if="isTabActive('processing')">

                    <div class="grid g-kpi5">
                        <div class="aqua-card kpi-tile" t-on-click="onDrillTotalProcessingOrders">
                            <div class="stat-top"><div class="stat-icon" style="background:var(--blue-tint); color:var(--blue);"><i class="fa fa-clipboard"/></div></div>
                            <div><div class="stat-val" t-esc="fmtNum(state.total_processing_orders)"/><div class="stat-lbl">Processing orders</div></div>
                        </div>
                        <div class="aqua-card kpi-tile" t-on-click="onDrillTotalProcessingOrders">
                            <div class="stat-top"><div class="stat-icon" style="background:var(--teal-tint); color:var(--teal);"><i class="fa fa-sign-in"/></div></div>
                            <div><div class="stat-val" t-esc="fmtTon(state.total_input_qty)"/><div class="stat-lbl">Total input qty</div></div>
                        </div>
                        <div class="aqua-card kpi-tile" t-on-click="onDrillWipStock">
                            <div class="stat-top"><div class="stat-icon" style="background:var(--amber-tint); color:var(--amber);"><i class="fa fa-spinner"/></div></div>
                            <div><div class="stat-val" t-esc="fmtNum(state.wip_stock_kg) + ' kg'"/><div class="stat-lbl">Staged for processing</div></div>
                        </div>
                        <div class="aqua-card kpi-tile">
                            <div class="stat-top"><div class="stat-icon" style="background:var(--purple-tint); color:var(--purple);"><i class="fa fa-snowflake-o"/></div></div>
                            <div><div class="stat-val" t-esc="fmtNum(state.active_blast_freeze_count)"/><div class="stat-lbl">Active blast freeze cycles</div></div>
                        </div>
                        <div class="aqua-card kpi-tile" t-on-click="() => this.onProcessingStatusChartClick({label: 'Done'})">
                            <div class="stat-top"><div class="stat-icon" style="background:var(--green-tint); color:var(--green);"><i class="fa fa-check"/></div></div>
                            <div><div class="stat-val" t-esc="fmtPct(state.total_processing_orders ? (procgOrderedByStatus.done / state.total_processing_orders * 100) : 0)"/><div class="stat-lbl">Orders done</div></div>
                        </div>
                    </div>

                    <div class="grid" style="grid-template-columns:1fr; margin-bottom:18px;">
                        <div class="aqua-card">
                            <h3>Staged for processing (WIP) by product</h3>
                            <p class="muted">Material currently staged ahead of processing</p>
                            <div class="hbar-chart">
                                <t t-foreach="procgWipRows" t-as="row" t-key="row.label">
                                    <div class="hbar-row" t-on-click="() => this.onWipStockByProductChartClick({label: row.label})" style="cursor:pointer;">
                                        <span class="hbar-cat" t-esc="row.label"/>
                                        <div class="hbar-track"><div class="hbar-fill" t-attf-style="width:{{barPct(row.value, procgWipRows)}}%; background:{{row.color}};"/></div>
                                    </div>
                                </t>
                                <div class="o_aqua_empty" t-if="!procgWipRows.length">Nothing currently staged for processing.</div>
                            </div>
                        </div>
                    </div>

                    <div class="grid g-mid-even">
                        <div class="aqua-card">
                            <h3>Processing order status</h3>
                            <p class="muted">Confirmed → in progress → done</p>
                            <div class="donut-wrap">
                                <svg width="150" height="150" viewBox="0 0 150 150">
                                    <circle cx="75" cy="75" r="58" fill="none" stroke="#EEF0F3" stroke-width="20"/>
                                    <t t-foreach="procgStatusSegments" t-as="seg" t-key="seg.label">
                                        <circle cx="75" cy="75" r="58" fill="none" stroke-width="20" stroke-linecap="round" style="cursor:pointer;"
                                                t-att-stroke="seg.color" t-att-stroke-dasharray="seg.dasharray" t-att-stroke-dashoffset="seg.dashoffset"
                                                transform="rotate(-90 75 75)"
                                                t-on-click="() => this.onProcessingStatusChartClick({label: seg.label})"
                                                t-on-mouseenter="(ev) => this.onSvgPointEnter(ev, seg.label + ': ' + this.fmtNum(seg.value))"
                                                t-on-mouseleave="() => this.onSvgPointLeave()"/>
                                    </t>
                                    <text x="75" y="71" text-anchor="middle" font-size="12" fill="var(--ink-mute)" font-family="Inter">Orders</text>
                                    <text x="75" y="90" text-anchor="middle" font-size="18" font-weight="700" fill="var(--ink)" font-family="Inter" t-esc="fmtNum(state.total_processing_orders)"/>
                                </svg>
                            </div>
                            <div class="legend">
                                <t t-foreach="state.processing_status_breakdown" t-as="row" t-key="row.label">
                                    <div t-att-class="'legend-row ' + seriesLegendClass('processing_status', row.label)" t-on-click="() => this.toggleSeries('processing_status', row.label)" style="cursor:pointer;">
                                        <div class="lk"><span class="legend-dot" t-attf-style="background:{{statusColor(row.label)}};"/><t t-esc="row.label"/></div>
                                        <t t-esc="row.value"/>
                                    </div>
                                </t>
                            </div>
                        </div>

                        <div class="aqua-card">
                            <h3>Input weight by species</h3>
                            <p class="muted">Most recent processing orders</p>
                            <div style="margin-top:4px;">
                                <t t-foreach="procgInputSpeciesRows" t-as="row" t-key="row.label">
                                    <div class="line-row" t-on-click="() => this.onInputQtyBySpeciesChartClick({label: row.label})" style="cursor:pointer;">
                                        <span class="lbl" t-esc="row.label"/>
                                        <div class="track"><div class="fill" t-attf-style="width:{{barPct(row.value, procgInputSpeciesRows)}}%; background:{{row.color}};"/></div>
                                        <span class="pct" t-esc="fmtTon(row.value)"/>
                                    </div>
                                </t>
                            </div>
                        </div>

                        <div class="aqua-card">
                            <h3>Blast freeze status</h3>
                            <p class="muted">Cycle status across cold rooms</p>
                            <div class="donut-wrap">
                                <svg width="150" height="150" viewBox="0 0 150 150">
                                    <circle cx="75" cy="75" r="58" fill="none" stroke="#EEF0F3" stroke-width="20"/>
                                    <t t-foreach="procgBlastFreezeSegments" t-as="seg" t-key="seg.label">
                                        <circle cx="75" cy="75" r="58" fill="none" stroke-width="20" stroke-linecap="round"
                                                t-att-stroke="seg.color" t-att-stroke-dasharray="seg.dasharray" t-att-stroke-dashoffset="seg.dashoffset"
                                                transform="rotate(-90 75 75)"/>
                                    </t>
                                    <text x="75" y="71" text-anchor="middle" font-size="12" fill="var(--ink-mute)" font-family="Inter">Cycles</text>
                                    <text x="75" y="90" text-anchor="middle" font-size="18" font-weight="700" fill="var(--ink)" font-family="Inter" t-esc="fmtNum(state.active_blast_freeze_count)"/>
                                </svg>
                            </div>
                            <div class="legend">
                                <t t-foreach="state.blast_freeze_status" t-as="row" t-key="row.label">
                                    <div t-att-class="'legend-row ' + seriesLegendClass('blast_freeze', row.label)" t-on-click="() => this.toggleSeries('blast_freeze', row.label)" style="cursor:pointer;">
                                        <div class="lk"><span class="legend-dot" t-attf-style="background:{{statusColor(row.label)}};"/><t t-esc="row.label"/></div>
                                        <t t-esc="row.value"/>
                                    </div>
                                </t>
                            </div>
                        </div>
                    </div>

                    <div class="grid g-bottom">
                        <div class="aqua-card">
                            <h3>Work-in-progress by product</h3>
                            <p class="muted">Material staged in pre-/post-production</p>
                            <t t-foreach="state.wip_stock_by_product" t-as="row" t-key="row.label">
                                <div class="wip-row" t-on-click="() => this.onWipStockByProductChartClick({label: row.label})" style="cursor:pointer;">
                                    <span t-esc="row.label"/><b t-esc="fmtTon(row.value)"/>
                                </div>
                            </t>
                            <div class="o_aqua_empty" t-if="!state.wip_stock_by_product.length">Nothing currently staged.</div>
                        </div>
                        <div class="aqua-card">
                            <h3>Order lifecycle flow</h3>
                            <p class="muted" t-esc="'Where the ' + fmtNum(state.total_processing_orders) + ' processing orders sit'"/>
                            <div class="stage-flow">
                                <div class="stage-box" t-on-click="() => this.onProcessingStatusChartClick({label: 'Confirmed'})" style="cursor:pointer;">
                                    <div class="n" t-esc="procgOrderedByStatus.confirmed"/><div class="l">Confirmed</div>
                                </div>
                                <i class="fa fa-arrow-right stage-arrow"/>
                                <div class="stage-box" t-on-click="() => this.onProcessingStatusChartClick({label: 'In Progress'})" style="cursor:pointer;">
                                    <div class="n" t-esc="procgOrderedByStatus.inProgress"/><div class="l">In progress</div>
                                </div>
                                <i class="fa fa-arrow-right stage-arrow"/>
                                <div class="stage-box" t-on-click="() => this.onProcessingStatusChartClick({label: 'Done'})" style="cursor:pointer;">
                                    <div class="n" t-esc="procgOrderedByStatus.done"/><div class="l">Done</div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="aqua-card">
                        <div class="card-head-row"><h3>Recent processing orders</h3></div>
                        <p class="muted">Intake through to current status</p>
                        <div class="table-scroll">
                        <table class="dtable" t-if="state.recent_processing_table.length">
                            <thead><tr><th>Order</th><th>Species</th><th>Catch receipt</th><th>Input qty</th><th>IPQC status</th><th>Status</th></tr></thead>
                            <tbody>
                                <t t-foreach="state.recent_processing_table" t-as="row" t-key="row.id">
                                    <tr t-on-click="() => this.onRecentProcessingRowClick(row.id)">
                                        <td t-esc="row.name"/>
                                        <td class="soft" t-esc="row.species"/>
                                        <td class="soft" t-esc="row.catch_receipt"/>
                                        <td t-esc="row.qty_input"/>
                                        <td><span t-att-class="'pill-sm ' + badgeClass(row.ipqc_status)" t-esc="row.ipqc_status"/></td>
                                        <td><span t-att-class="'pill-sm ' + badgeClass(row.state)" t-esc="row.state"/></td>
                                    </tr>
                                </t>
                            </tbody>
                        </table>
                        </div>
                        <div class="o_aqua_empty" t-if="!state.recent_processing_table.length">No processing orders recorded yet.</div>
                    </div>
                </t>

                <!-- ══════════════════════ QUALITY CONTROL TAB ══════════════════════ -->
                <t t-if="isTabActive('quality')">

                    <div class="grid g-kpi6">
                        <div class="aqua-card kpi-tile" t-on-click="onDrillQcTotal">
                            <div class="stat-top"><div class="stat-icon" style="background:var(--blue-tint); color:var(--blue);"><i class="fa fa-list-alt"/></div></div>
                            <div><div class="stat-val" t-esc="fmtNum(state.qc_total)"/><div class="stat-lbl">Total checks</div></div>
                        </div>
                        <div class="aqua-card kpi-tile" t-on-click="onDrillQcFail">
                            <div class="stat-top"><div class="stat-icon" style="background:var(--coral-tint); color:var(--coral);"><i class="fa fa-times"/></div></div>
                            <div><div class="stat-val" t-esc="fmtNum(state.qc_fail_count)"/><div class="stat-lbl">Failed</div></div>
                        </div>
                        <div class="aqua-card kpi-tile">
                            <div class="stat-top"><div class="stat-icon" style="background:var(--amber-tint); color:var(--amber);"><i class="fa fa-hourglass-half"/></div></div>
                            <div><div class="stat-val" t-esc="fmtNum(state.qc_todo_count)"/><div class="stat-lbl">To do</div></div>
                        </div>
                        <div class="aqua-card kpi-tile" t-on-click="onDrillQcHold">
                            <div class="stat-top"><div class="stat-icon" style="background:var(--purple-tint); color:var(--purple);"><i class="fa fa-pause-circle"/></div></div>
                            <div><div class="stat-val" t-esc="fmtNum(state.qc_hold_count)"/><div class="stat-lbl">On hold</div></div>
                        </div>
                        <div class="aqua-card kpi-tile">
                            <div class="stat-top"><div class="stat-icon" style="background:var(--teal-tint); color:var(--teal);"><i class="fa fa-flask"/></div></div>
                            <div><div class="stat-val" t-esc="fmt1(state.avg_histamine_ppm) + ' ppm'"/><div class="stat-lbl">Avg histamine</div></div>
                        </div>
                        <div class="aqua-card kpi-tile">
                            <div class="stat-top"><div class="stat-icon" style="background:var(--green-tint); color:var(--green);"><i class="fa fa-star"/></div></div>
                            <div><div class="stat-val" t-esc="fmt1(state.avg_sensory_score) + ' / 10'"/><div class="stat-lbl">Avg sensory score</div></div>
                        </div>
                    </div>

                    <div class="grid g-kpi3">
                        <div class="aqua-card stage-card" t-on-click="onDrillIqcTotal">
                            <div class="top"><span class="name">IQC — Incoming (Receiving)</span><i class="fa fa-truck" style="color:var(--blue);"/></div>
                            <div class="rate" style="color:var(--blue);" t-esc="fmtPct(state.iqc_pass_rate)"/>
                            <div class="n"><t t-esc="state.iqc_total"/> checks · pass rate</div>
                        </div>
                        <div class="aqua-card stage-card" t-on-click="onDrillIpqcTotal">
                            <div class="top"><span class="name">IPQC — In-Process (Manufacturing)</span><i class="fa fa-cogs" style="color:var(--teal);"/></div>
                            <div class="rate" style="color:var(--teal);" t-esc="fmtPct(state.ipqc_pass_rate)"/>
                            <div class="n"><t t-esc="state.ipqc_total"/> checks · pass rate</div>
                        </div>
                        <div class="aqua-card stage-card" t-on-click="onDrillFinalQcTotal">
                            <div class="top"><span class="name">Final QC (Pre-Shipment)</span><i class="fa fa-ship" style="color:var(--purple);"/></div>
                            <div class="rate" style="color:var(--purple);" t-esc="fmtPct(state.final_qc_pass_rate)"/>
                            <div class="n"><t t-esc="state.final_qc_total"/> checks · pass rate</div>
                        </div>
                    </div>

                    <div class="grid g-mid-even">
                        <div class="aqua-card">
                            <h3>Checks by stage</h3>
                            <p class="muted">IQC · IPQC · Final QC</p>
                            <div class="donut-wrap">
                                <svg width="150" height="150" viewBox="0 0 150 150">
                                    <circle cx="75" cy="75" r="58" fill="none" stroke="#EEF0F3" stroke-width="20"/>
                                    <t t-foreach="qQcStageSegments" t-as="seg" t-key="seg.label">
                                        <circle cx="75" cy="75" r="58" fill="none" stroke-width="20" stroke-linecap="round" style="cursor:pointer;"
                                                t-att-stroke="seg.color" t-att-stroke-dasharray="seg.dasharray" t-att-stroke-dashoffset="seg.dashoffset"
                                                transform="rotate(-90 75 75)"
                                                t-on-click="() => this.onQcStageChartClick({label: seg.label})"
                                                t-on-mouseenter="(ev) => this.onSvgPointEnter(ev, seg.label + ': ' + this.fmtNum(seg.value))"
                                                t-on-mouseleave="() => this.onSvgPointLeave()"/>
                                    </t>
                                    <text x="75" y="71" text-anchor="middle" font-size="12" fill="var(--ink-mute)" font-family="Inter">Checks</text>
                                    <text x="75" y="90" text-anchor="middle" font-size="18" font-weight="700" fill="var(--ink)" font-family="Inter" t-esc="fmtNum(state.qc_total)"/>
                                </svg>
                            </div>
                            <div class="legend">
                                <t t-foreach="qQcStageRows" t-as="seg" t-key="seg.label">
                                    <div t-att-class="'legend-row ' + seriesLegendClass('qc_stage', seg.label)" t-on-click="() => this.toggleSeries('qc_stage', seg.label)" style="cursor:pointer;">
                                        <div class="lk"><span class="legend-dot" t-attf-style="background:{{seg.color}};"/><t t-esc="seg.label"/></div>
                                        <t t-esc="seg.value"/>
                                    </div>
                                </t>
                            </div>
                        </div>

                        <div class="aqua-card">
                            <h3>Intake decisions</h3>
                            <p class="muted">Raw material stage only</p>
                            <div class="donut-wrap">
                                <svg width="150" height="150" viewBox="0 0 150 150">
                                    <circle cx="75" cy="75" r="58" fill="none" stroke="#EEF0F3" stroke-width="20"/>
                                    <t t-foreach="qIntakeDecisionSegments" t-as="seg" t-key="seg.label">
                                        <circle cx="75" cy="75" r="58" fill="none" stroke-width="20" stroke-linecap="round" style="cursor:pointer;"
                                                t-att-stroke="seg.color" t-att-stroke-dasharray="seg.dasharray" t-att-stroke-dashoffset="seg.dashoffset"
                                                transform="rotate(-90 75 75)"
                                                t-on-click="() => this.onIntakeDecisionChartClick({label: seg.label})"
                                                t-on-mouseenter="(ev) => this.onSvgPointEnter(ev, seg.label + ': ' + this.fmtNum(seg.value))"
                                                t-on-mouseleave="() => this.onSvgPointLeave()"/>
                                    </t>
                                    <text x="75" y="71" text-anchor="middle" font-size="12" fill="var(--ink-mute)" font-family="Inter">Total</text>
                                    <text x="75" y="90" text-anchor="middle" font-size="18" font-weight="700" fill="var(--ink)" font-family="Inter" t-esc="fmtNum(state.iqc_total)"/>
                                </svg>
                            </div>
                            <div class="legend">
                                <t t-foreach="qIntakeDecisionRows" t-as="seg" t-key="seg.label">
                                    <div t-att-class="'legend-row ' + seriesLegendClass('intake_decision', seg.label)" t-on-click="() => this.toggleSeries('intake_decision', seg.label)" style="cursor:pointer;">
                                        <div class="lk"><span class="legend-dot" t-attf-style="background:{{seg.color}};"/><t t-esc="seg.label"/></div>
                                        <t t-esc="seg.value"/>
                                    </div>
                                </t>
                            </div>
                        </div>

                        <div class="aqua-card">
                            <h3>Rejected qty by species</h3>
                            <p class="muted">Raw material stage rejections</p>
                            <div style="margin-top:4px;">
                                <t t-foreach="qRejectedBySpeciesRows" t-as="row" t-key="row.label">
                                    <div class="line-row" t-on-click="() => this.onRejectedQtyBySpeciesChartClick({label: row.label})" style="cursor:pointer;">
                                        <span class="lbl" t-esc="row.label"/>
                                        <div class="track"><div class="fill" t-attf-style="width:{{barPct(row.value, qRejectedBySpeciesRows)}}%; background:{{row.color}};"/></div>
                                        <span class="pct" t-esc="fmtTon(row.value)"/>
                                    </div>
                                </t>
                                <div class="o_aqua_empty" t-if="!qRejectedBySpeciesRows.length">No rejected raw material recorded yet.</div>
                            </div>
                        </div>
                    </div>

                    <div class="grid" style="grid-template-columns:1fr; margin-bottom:18px;">
                        <div class="aqua-card">
                            <h3>QC checks trend</h3>
                            <p class="muted" t-esc="qcTrendTitle"/>
                            <svg width="100%" height="190" viewBox="0 0 580 200" style="margin-top:8px;" t-if="state.qc_trend.length">
                                <t t-set="g" t-value="qTrendGeom"/>
                                <t t-foreach="g.gridLines" t-as="gl" t-key="gl_index">
                                    <line t-att-x1="g.padL" t-att-y1="gl.y" t-att-x2="g.width - g.padR" t-att-y2="gl.y" stroke="var(--line)"/>
                                    <text t-att-x="g.padL - 10" t-att-y="gl.y + 4" text-anchor="end" font-size="10" fill="var(--ink-mute)" font-family="Inter" t-esc="gl.label"/>
                                </t>
                                <path t-att-d="g.areaPath" fill="var(--green)" opacity="0.15"/>
                                <polyline t-att-points="g.polyline" fill="none" stroke="var(--green)" stroke-width="2.5"/>
                                <t t-foreach="g.points" t-as="p" t-key="p_index">
                                    <circle t-att-cx="p.x" t-att-cy="p.y" r="3.5" fill="var(--green)"/>
                                    <circle t-att-cx="p.x" t-att-cy="p.y" r="11" fill="transparent" style="cursor:pointer;"
                                            t-on-click="() => this.onQcTrendChartClick({label: p.label})"
                                            t-on-mouseenter="(ev) => this.onSvgPointEnter(ev, p.label + ': ' + this.fmtNum(p.value) + ' check(s)')"
                                            t-on-mouseleave="() => this.onSvgPointLeave()"/>
                                    <text t-att-x="p.x" t-att-y="g.baseline + 18" text-anchor="middle" font-size="10.5" fill="var(--ink-mute)" font-family="Inter" t-esc="p.label"/>
                                </t>
                            </svg>
                            <div class="o_aqua_empty" t-else="">Not enough data yet for a trend line.</div>
                        </div>
                    </div>

                    <div class="grid g-bottom-wide">
                        <div class="aqua-card">
                            <h3>IPQC by shop-floor operation</h3>
                            <p class="muted">Pass vs fail per station</p>
                            <t t-foreach="qIpqcByOperationRows" t-as="row" t-key="row.label">
                                <div class="pair-row" t-on-click="() => this.onIpqcByOperationChartClick({label: row.label})" style="cursor:pointer;">
                                    <span class="lbl" t-esc="row.label"/>
                                    <div class="pair-track">
                                        <div class="seg" t-if="isSeriesVisible('ipqc_by_operation', 'pass')" t-attf-style="width:{{row.passPct}}%; background:var(--green);"/>
                                        <div class="seg" t-if="isSeriesVisible('ipqc_by_operation', 'fail')" t-attf-style="width:{{row.failPct}}%; background:var(--coral);"/>
                                    </div>
                                    <span class="pair-count"><t t-esc="row.pass"/> / <t t-esc="row.fail"/></span>
                                </div>
                            </t>
                            <div class="o_aqua_empty" t-if="!qIpqcByOperationRows.length">No in-process checks recorded yet.</div>
                            <div class="pair-legend">
                                <div t-att-class="seriesLegendClass('ipqc_by_operation', 'pass')" style="cursor:pointer;" t-on-click="() => this.toggleSeries('ipqc_by_operation', 'pass')"><span class="d" style="background:var(--green);"/>Pass</div>
                                <div t-att-class="seriesLegendClass('ipqc_by_operation', 'fail')" style="cursor:pointer;" t-on-click="() => this.toggleSeries('ipqc_by_operation', 'fail')"><span class="d" style="background:var(--coral);"/>Fail</div>
                            </div>
                        </div>

                        <div class="aqua-card">
                            <h3>Residue screening</h3>
                            <p class="muted">Antibiotic and sulphite results</p>
                            <t t-foreach="qResidueRows" t-as="row" t-key="row.title">
                                <div class="pair-row">
                                    <span class="lbl" t-esc="row.title"/>
                                    <div class="pair-track">
                                        <t t-foreach="row.segs" t-as="seg" t-key="seg.label">
                                            <div class="seg" t-if="isSeriesVisible('residue_screening', seg.label)" t-attf-style="width:{{seg.pct}}%; background:{{seg.color}};"/>
                                        </t>
                                    </div>
                                </div>
                            </t>
                            <div class="o_aqua_empty" t-if="!qResidueRows.length">No residue screening data yet.</div>
                            <div class="pair-legend">
                                <div t-att-class="seriesLegendClass('residue_screening', 'Not Tested')" style="cursor:pointer;" t-on-click="() => this.toggleSeries('residue_screening', 'Not Tested')"><span class="d" style="background:#D9DEE4;"/>Not tested</div>
                                <div t-att-class="seriesLegendClass('residue_screening', 'Not Detected')" style="cursor:pointer;" t-on-click="() => this.toggleSeries('residue_screening', 'Not Detected')"><span class="d" style="background:var(--green);"/>Not detected</div>
                                <div t-att-class="seriesLegendClass('residue_screening', 'Detected')" style="cursor:pointer;" t-on-click="() => this.toggleSeries('residue_screening', 'Detected')"><span class="d" style="background:var(--coral);"/>Detected</div>
                            </div>
                        </div>
                    </div>

                    <div class="aqua-card">
                        <div class="card-head-row"><h3>Recent quality checks</h3></div>
                        <p class="muted">Most recent inspections across all stages</p>
                        <div class="table-scroll">
                        <table class="dtable" t-if="state.recent_qc_table.length">
                            <thead><tr><th>Check</th><th>Stage</th><th>Source</th><th>Result</th><th>Histamine (ppm)</th><th>Core temp (°C)</th></tr></thead>
                            <tbody>
                                <t t-foreach="state.recent_qc_table" t-as="row" t-key="row.id">
                                    <tr t-on-click="() => this.onRecentQcRowClick(row.id)">
                                        <td t-esc="row.name"/>
                                        <td class="soft" t-esc="row.stage"/>
                                        <td class="soft" t-esc="row.source"/>
                                        <td><span t-att-class="'pill-sm ' + badgeClass(row.result)" t-esc="row.result"/></td>
                                        <td class="soft" t-esc="row.histamine_ppm"/>
                                        <td class="soft" t-esc="row.core_temp_c"/>
                                    </tr>
                                </t>
                            </tbody>
                        </table>
                        </div>
                        <div class="o_aqua_empty" t-if="!state.recent_qc_table.length">No quality checks recorded yet.</div>
                    </div>
                </t>

                <!-- ══════════════════════ BUDGET TAB ══════════════════════ -->
                <!-- Reads the currently-active Budget (Accounting > Budgets) for this
                     company, the same record configured under Analytic Accounts /
                     Budgetary Positions / Budgets. This tab is a read-only summary;
                     all editing still happens in Accounting. -->
                <t t-if="isTabActive('budget')">

                    <div class="aqua-card" style="margin-bottom:18px; cursor:pointer;" t-on-click="onOpenBudgetRecord">
                        <div class="budget-header">
                            <div>
                                <div class="name">
                                    <t t-if="state.budget_name" t-esc="state.budget_name"/>
                                    <t t-else="">No Active Budget</t>
                                </div>
                                <div class="period" t-if="state.budget_period_from">
                                    <t t-esc="state.budget_period_from"/> – <t t-esc="state.budget_period_to"/>
                                </div>
                            </div>
                            <span class="status-pill" t-att-class="'pill-sm ' + (['confirmed', 'validate', 'done'].includes((state.budget_state || '').toLowerCase()) ? 'aqua-badge--green' : 'aqua-badge--amber')" t-esc="budgetStateLabel"/>
                        </div>
                    </div>

                    <t t-if="!state.budget_id">
                        <div class="aqua-card"><div class="o_aqua_empty">No confirmed budget found for this company. Set one up under Accounting → Budgets.</div></div>
                    </t>

                    <t t-if="state.budget_id">
                        <div class="grid g-kpi5">
                            <div class="aqua-card kpi-tile" t-on-click="onOpenBudgetRecord">
                                <div class="stat-top"><div class="stat-icon" style="background:var(--blue-tint); color:var(--blue);"><i class="fa fa-bullseye"/></div></div>
                                <div><div class="stat-val" t-esc="fmtINR(state.budget_total_planned)"/><div class="stat-lbl">Total planned</div></div>
                            </div>
                            <div class="aqua-card kpi-tile" t-on-click="onOpenBudgetRecord">
                                <div class="stat-top"><div class="stat-icon" style="background:var(--teal-tint); color:var(--teal);"><i class="fa fa-inr"/></div></div>
                                <div><div class="stat-val" t-esc="fmtINR(state.budget_total_practical)"/><div class="stat-lbl">Actual (practical)</div></div>
                            </div>
                            <div class="aqua-card kpi-tile" t-on-click="onOpenBudgetRecord">
                                <div class="stat-top"><div class="stat-icon" style="background:var(--purple-tint); color:var(--purple);"><i class="fa fa-clock-o"/></div></div>
                                <div><div class="stat-val" t-esc="fmtINR(state.budget_total_theoretical)"/><div class="stat-lbl">Theoretical (to date)</div></div>
                            </div>
                            <div class="aqua-card kpi-tile" t-on-click="onOpenBudgetRecord">
                                <div class="stat-top"><div class="stat-icon" style="background:var(--green-tint); color:var(--green);"><i class="fa fa-line-chart"/></div></div>
                                <div><div class="stat-val" t-esc="fmtINR(state.budget_total_gross_margin)"/><div class="stat-lbl">Gross margin</div></div>
                            </div>
                            <div class="aqua-card kpi-tile" t-on-click="onOpenBudgetRecord">
                                <div class="stat-top"><div class="stat-icon" style="background:var(--amber-tint); color:var(--amber);"><i class="fa fa-percent"/></div></div>
                                <div><div class="stat-val" t-esc="fmtPct(state.budget_achievement_pct)"/><div class="stat-lbl">Achievement</div></div>
                            </div>
                        </div>

                        <div class="grid g-two">
                            <div class="aqua-card">
                                <h3>Achievement</h3>
                                <p class="muted">Practical vs theoretical spend, to date</p>
                                <div class="gauge-wrap">
                                    <svg width="220" height="130" viewBox="0 0 220 130">
                                        <path d="M15,120 A95,95 0 0 1 205,120" fill="none" stroke="#EEF0F3" stroke-width="18" stroke-linecap="round"/>
                                        <path d="M15,120 A95,95 0 0 1 205,120" fill="none" stroke="var(--blue)" stroke-width="18" stroke-linecap="round"
                                              t-att-stroke-dasharray="semiArcDash(state.budget_achievement_pct, 95)"/>
                                        <text x="110" y="100" text-anchor="middle" font-size="30" font-weight="700" fill="var(--ink)" font-family="Inter" t-esc="fmtPct(state.budget_achievement_pct)"/>
                                    </svg>
                                </div>
                                <p class="muted" style="text-align:center;">
                                    <t t-esc="fmtINR(state.budget_total_practical)"/> practical of <t t-esc="fmtINR(state.budget_total_theoretical)"/> theoretical
                                </p>
                            </div>

                            <div class="aqua-card">
                                <h3>Planned vs practical by cost center</h3>
                                <p class="muted">Top budget lines</p>
                                <t t-foreach="budgetPairRows" t-as="row" t-key="row.label">
                                    <div class="bar-pair-row" t-on-click="onOpenBudgetRecord" style="cursor:pointer;">
                                        <span class="lbl" t-esc="row.label"/>
                                        <div class="bars">
                                            <div class="bp" t-if="isSeriesVisible('budget_pair', 'planned')"><div class="track"><div class="fill" t-attf-style="width:{{row.plannedPct}}%; background:var(--blue);"/></div><span class="v" t-esc="fmtINR(row.planned)"/></div>
                                            <div class="bp" t-if="isSeriesVisible('budget_pair', 'practical')"><div class="track"><div class="fill" t-attf-style="width:{{row.practicalPct}}%; background:var(--teal);"/></div><span class="v" t-esc="fmtINR(row.practical)"/></div>
                                        </div>
                                    </div>
                                </t>
                                <div class="o_aqua_empty" t-if="!budgetPairRows.length">No budget lines added yet.</div>
                                <div class="pair-legend">
                                    <div t-att-class="seriesLegendClass('budget_pair', 'planned')" style="cursor:pointer;" t-on-click.stop="() => this.toggleSeries('budget_pair', 'planned')"><span class="d" style="background:var(--blue);"/>Planned</div>
                                    <div t-att-class="seriesLegendClass('budget_pair', 'practical')" style="cursor:pointer;" t-on-click.stop="() => this.toggleSeries('budget_pair', 'practical')"><span class="d" style="background:var(--teal);"/>Practical</div>
                                </div>
                            </div>
                        </div>

                        <div class="aqua-card">
                            <div class="card-head-row">
                                <h3>Budget lines</h3>
                                <span class="view-all" t-on-click="onOpenBudgetRecord">Open in Accounting</span>
                            </div>
                            <p class="muted">By cost center / analytic account</p>
                            <div class="table-scroll">
                            <table class="dtable" t-if="state.budget_lines_table.length">
                                <thead><tr><th>Cost center</th><th>Analytic account</th><th>Planned</th><th>Practical</th><th>Theoretical</th><th>Achievement</th><th>Gross margin</th></tr></thead>
                                <tbody>
                                    <t t-foreach="state.budget_lines_table" t-as="row" t-key="row_index">
                                        <tr t-on-click="onOpenBudgetRecord">
                                            <td t-esc="row.position"/>
                                            <td class="soft" t-esc="row.analytic_account"/>
                                            <td class="soft" t-esc="row.planned"/>
                                            <td t-esc="row.practical"/>
                                            <td class="soft" t-esc="row.theoretical"/>
                                            <td><span t-att-class="'pill-sm ' + (row.achievement >= 90 ? 'aqua-badge--green' : (row.achievement >= 75 ? 'aqua-badge--amber' : 'aqua-badge--red'))"><t t-esc="row.achievement"/>%</span></td>
                                            <td class="soft" t-esc="row.gross_margin"/>
                                        </tr>
                                    </t>
                                </tbody>
                            </table>
                            </div>
                            <div class="o_aqua_empty" t-if="!state.budget_lines_table.length">No budget lines added yet.</div>
                        </div>
                    </t>
                </t>

                <DrillPanel
                    isOpen="state.drill.isOpen"
                    title="state.drill.title"
                    model="state.drill.model"
                    records="state.drill.records"
                    columns="state.drill.columns"
                    isLoading="state.drill.loading"
                    onClose.bind="closeDrill"/>

                <!-- Shared hover tooltip for the hand-drawn SVG trend lines
                     (receipt/weight/spend/QC trends). position:fixed + viewport
                     coordinates set in onSvgPointEnter, so one instance here
                     covers every trend card regardless of scroll position. -->
                <div t-if="svgTooltip.visible" class="aqua-chart-tooltip aqua-chart-tooltip--fixed"
                     t-attf-style="left:{{svgTooltip.x}}px; top:{{svgTooltip.y}}px;">
                    <div class="aqua-chart-tooltip-row">
                        <span class="aqua-chart-tooltip-value" t-esc="svgTooltip.text"/>
                    </div>
                </div>

            </div>
        </div>
    </t>
</templates>