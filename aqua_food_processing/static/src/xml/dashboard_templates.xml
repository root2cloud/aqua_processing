<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="aqua_food_processing.DashboardMain">
        <div class="o_aqua_dashboard">
            <div class="o_aqua_header">
                <div class="o_aqua_header__row">
                    <div class="o_aqua_header__title">
                        <i class="fa fa-anchor"/>
                        <h2>Aqua Processing</h2>
                    </div>
                    <FilterBar onFilterChange.bind="onFilterChange" onRefresh.bind="onRefresh" isLoading="state.isLoading"/>
                </div>
            </div>

            <!-- TAB BAR -->
            <div class="o_aqua_tabs">
                <button type="button"
                        class="o_aqua_tab_btn"
                        t-att-class="{ o_aqua_tab_active: isTabActive('overview') }"
                        t-on-click="() => this.setActiveTab('overview')">
                    <i class="fa fa-th-large mr-1"/> Overview
                </button>
                <button type="button"
                        class="o_aqua_tab_btn"
                        t-att-class="{ o_aqua_tab_active: isTabActive('procurement') }"
                        t-on-click="() => this.setActiveTab('procurement')">
                    <i class="fa fa-truck mr-1"/> Procurement
                </button>
                <button type="button"
                        class="o_aqua_tab_btn"
                        t-att-class="{ o_aqua_tab_active: isTabActive('processing') }"
                        t-on-click="() => this.setActiveTab('processing')">
                    <i class="fa fa-industry mr-1"/> Processing
                </button>
                <button type="button"
                        class="o_aqua_tab_btn"
                        t-att-class="{ o_aqua_tab_active: isTabActive('quality') }"
                        t-on-click="() => this.setActiveTab('quality')">
                    <i class="fa fa-flask mr-1"/> Quality Control
                </button>
                <button type="button"
                        class="o_aqua_tab_btn"
                        t-att-class="{ o_aqua_tab_active: isTabActive('budget') }"
                        t-on-click="() => this.setActiveTab('budget')">
                    <i class="fa fa-money mr-1"/> Budget
                </button>
            </div>

            <!-- ══════════════════════ OVERVIEW TAB ══════════════════════ -->
            <t t-if="isTabActive('overview')">
                <div class="o_aqua_section_label">At a Glance — One Number per Department</div>
                <div class="o_aqua_kpi_row">
                    <KpiTile
                        label="'Total Catch Receipts'"
                        value="state.total_receipts"
                        valueFmt="'number'"
                        icon="'fa-truck'"
                        color="'#2C7A7B'"
                        sparklineData="receiptsSparkline"
                        isLoading="state.isLoading"
                        changePct="state.comparison_pct.total_receipts"
                        compareLabel="state.comparison_label"
                        onDrill.bind="onDrillTotalReceipts"/>
                    <KpiTile
                        label="'Current Stock On Hand (kg)'"
                        value="state.total_stock_on_hand"
                        valueFmt="'number'"
                        icon="'fa-cubes'"
                        color="'#3182CE'"
                        sparklineData="stockSparkline"
                        isLoading="state.isLoading"
                        onDrill.bind="onDrillTotalStockOnHand"/>
                    <KpiTile
                        label="'QC Pass Rate'"
                        value="state.qc_pass_rate"
                        valueFmt="'pct'"
                        icon="'fa-flask'"
                        color="'#38A169'"
                        isLoading="state.isLoading"
                        changePct="state.comparison_pct.qc_pass_rate"
                        compareLabel="state.comparison_label"
                        onDrill.bind="onDrillQcPassRate"/>
                    <KpiTile
                        label="'QC On Hold'"
                        value="state.qc_hold_count"
                        valueFmt="'number'"
                        icon="'fa-pause-circle'"
                        color="'#D69E2E'"
                        isLoading="state.isLoading"
                        onDrill.bind="onDrillQcHold"/>
                    <KpiTile
                        label="'On-time Dispatch Rate'"
                        value="state.on_time_dispatch_rate"
                        valueFmt="'pct'"
                        icon="'fa-ship'"
                        color="'#C53030'"
                        isLoading="state.isLoading"
                        onDrill.bind="onDrillDispatchRate"/>
                </div>

                <div class="o_aqua_section_label">Trends &amp; Breakdown</div>
                <div class="o_aqua_chart_row">
                    <div class="o_aqua_panel_wide">
                        <t t-if="state.purchase_to_stock_funnel.length">
                            <ChartWidget
                                title="'Purchase-to-Stock Flow (kg)'"
                                chartType="'bar'"
                                data="purchaseToStockFunnelChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onPurchaseToStockFunnelChartClick"
                                exportFilename="'aqua_purchase_to_stock_funnel'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Purchase-to-Stock Flow (kg)</div></div><div class="o_aqua_empty">No catch receipts recorded yet.</div></div></t>
                    </div>
                    <div class="o_aqua_panel_narrow">
                        <t t-if="state.qc_breakdown.length">
                            <ChartWidget
                                title="'QC Result Breakdown'"
                                chartType="'doughnut'"
                                data="qcChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onQcChartClick"
                                exportFilename="'aqua_qc_breakdown'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">QC Result Breakdown</div></div><div class="o_aqua_empty">No QC tests recorded yet.</div></div></t>
                    </div>
                </div>

                <div class="o_aqua_chart_row">
                    <div class="o_aqua_panel_narrow">
                        <t t-if="state.shipment_breakdown.length">
                            <ChartWidget
                                title="'Shipment Status'"
                                chartType="'doughnut'"
                                data="shipmentChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onShipmentChartClick"
                                exportFilename="'aqua_shipment_status'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Shipment Status</div></div><div class="o_aqua_empty">No shipments recorded yet.</div></div></t>
                    </div>
                    <div class="o_aqua_panel_wide">
                        <t t-if="state.receipt_trend.length">
                            <ChartWidget
                                title="receiptTrendTitle"
                                chartType="'line'"
                                data="receiptTrendChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onReceiptTrendChartClick"
                                exportFilename="'aqua_receipt_trend'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title" t-esc="receiptTrendTitle"/></div><div class="o_aqua_empty">Not enough data yet for a trend line.</div></div></t>
                    </div>
                </div>

                <!-- Headline chart matching the KPI row above: Quality's IQC/IPQC/Final
                     split, otherwise only visible if you click into that tab. -->
                <div class="o_aqua_chart_row">
                    <div class="o_aqua_panel_narrow">
                        <t t-if="state.qc_stage_breakdown.length">
                            <ChartWidget
                                title="'Checks by Stage — IQC / IPQC / Final'"
                                chartType="'doughnut'"
                                data="qcStageChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onQcStageChartClick"
                                exportFilename="'aqua_qc_stage_breakdown'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Checks by Stage — IQC / IPQC / Final</div></div><div class="o_aqua_empty">No QC tests recorded yet.</div></div></t>
                    </div>
                </div>
            </t>

            <!-- ══════════════════════ PROCUREMENT TAB ══════════════════════ -->
            <!-- Covers the full purchase -> storage flow: Purchase Order -> Catch
                 Receipt -> Deliveries -> weight reconciliation -> vendor performance. -->
            <t t-if="isTabActive('procurement')">
                <div class="o_aqua_section_label">Purchase Activity</div>
                <div class="o_aqua_kpi_row">
                    <KpiTile
                        label="'Total Catch Receipts'"
                        value="state.total_receipts"
                        valueFmt="'number'"
                        icon="'fa-truck'"
                        color="'#2C7A7B'"
                        sparklineData="receiptsSparkline"
                        isLoading="state.isLoading"
                        changePct="state.comparison_pct.total_receipts"
                        compareLabel="state.comparison_label"
                        onDrill.bind="onDrillTotalReceipts"/>
                    <KpiTile
                        label="'Completed Receipts'"
                        value="state.accepted_receipts"
                        valueFmt="'number'"
                        icon="'fa-check-circle'"
                        color="'#38A169'"
                        isLoading="state.isLoading"
                        onDrill.bind="onDrillAcceptedReceipts"/>
                    <KpiTile
                        label="'Cancelled / Rejected'"
                        value="state.cancelled_receipts"
                        valueFmt="'number'"
                        icon="'fa-times-circle'"
                        color="'#C53030'"
                        isLoading="state.isLoading"
                        onDrill.bind="onDrillCancelledReceipts"/>
                    <KpiTile
                        label="'Total Weight Received (kg)'"
                        value="state.total_weight_received"
                        valueFmt="'number'"
                        icon="'fa-balance-scale'"
                        color="'#3182CE'"
                        isLoading="state.isLoading"
                        changePct="state.comparison_pct.total_weight_received"
                        compareLabel="state.comparison_label"
                        onDrill.bind="onDrillTotalWeightReceived"/>
                    <KpiTile
                        label="'Total Purchase Spend (₹)'"
                        value="state.total_purchase_spend"
                        valueFmt="'number'"
                        icon="'fa-money'"
                        color="'#805AD5'"
                        isLoading="state.isLoading"
                        changePct="state.comparison_pct.total_purchase_spend"
                        compareLabel="state.comparison_label"
                        onDrill.bind="onDrillTotalPurchaseSpend"/>
                    <KpiTile
                        label="'Avg Price / kg (₹)'"
                        value="state.avg_price_per_kg"
                        valueFmt="'number'"
                        icon="'fa-inr'"
                        color="'#D69E2E'"
                        isLoading="state.isLoading"
                        changePct="state.comparison_pct.avg_price_per_kg"
                        compareLabel="state.comparison_label"
                        onDrill.bind="onDrillTotalPurchaseSpend"/>
                    <KpiTile
                        label="'Active Vendors'"
                        value="state.active_vendor_count"
                        valueFmt="'number'"
                        icon="'fa-users'"
                        color="'#D69E2E'"
                        isLoading="state.isLoading"/>
                </div>

                <!-- Live Inventory: current stock position, read straight off stock.quant:
                     what actually made it through Receiving, QC, and Storage, and is sitting
                     on hand right now. Manufacturing work-in-progress (Pre-/Post-Production)
                     is deliberately excluded here; that belongs on the Processing tab. -->
                <div class="o_aqua_section_label">Live Inventory (Current Stock)</div>
                <div class="o_aqua_kpi_row">
                    <KpiTile
                        label="'Current Stock On Hand (kg)'"
                        value="state.total_stock_on_hand"
                        valueFmt="'number'"
                        icon="'fa-cubes'"
                        color="'#38A169'"
                        sparklineData="stockSparkline"
                        isLoading="state.isLoading"
                        onDrill.bind="onDrillTotalStockOnHand"/>
                    <KpiTile
                        label="'Stock Value (₹)'"
                        value="state.stock_value"
                        valueFmt="'number'"
                        icon="'fa-inr'"
                        color="'#2C7A7B'"
                        isLoading="state.isLoading"
                        onDrill.bind="onDrillStockValue"/>
                </div>

                <div class="o_aqua_chart_row">
                    <div class="o_aqua_panel_wide">
                        <t t-if="state.current_stock_by_product.length">
                            <ChartWidget
                                title="'Current Stock by Product (kg)'"
                                chartType="'horizontalBar'"
                                data="currentStockByProductChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onCurrentStockByProductChartClick"
                                exportFilename="'aqua_stock_by_product'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Current Stock by Product (kg)</div></div><div class="o_aqua_empty">No stock on hand yet.</div></div></t>
                    </div>
                    <div class="o_aqua_panel_narrow">
                        <t t-if="state.current_stock_by_location.length">
                            <ChartWidget
                                title="'Stock by Location — Pipeline (kg)'"
                                chartType="'horizontalBar'"
                                data="currentStockByLocationChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onCurrentStockByLocationChartClick"
                                exportFilename="'aqua_stock_by_location'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Stock by Location — Pipeline (kg)</div></div><div class="o_aqua_empty">No stock on hand yet.</div></div></t>
                    </div>
                </div>

                <div class="o_aqua_chart_row">
                    <div class="o_aqua_panel_narrow">
                        <t t-if="state.purchase_to_stock_funnel.length">
                            <ChartWidget
                                title="'Purchase-to-Stock Flow (kg)'"
                                chartType="'bar'"
                                data="purchaseToStockFunnelChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onPurchaseToStockFunnelChartClick"
                                exportFilename="'aqua_purchase_to_stock_funnel'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Purchase-to-Stock Flow (kg)</div></div><div class="o_aqua_empty">No catch receipts recorded yet.</div></div></t>
                    </div>
                    <div class="o_aqua_panel_wide">
                        <t t-if="state.daily_weight_trend.length">
                            <ChartWidget
                                title="weightReceivedTrendTitle"
                                chartType="'line'"
                                data="dailyWeightTrendChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onDailyWeightTrendChartClick"
                                exportFilename="'aqua_daily_weight_received'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title" t-esc="weightReceivedTrendTitle"/></div><div class="o_aqua_empty">Not enough data yet for a trend line.</div></div></t>
                    </div>
                </div>

                <div class="o_aqua_section_label">Cost &amp; Vendor Performance</div>
                <div class="o_aqua_chart_row">
                    <div class="o_aqua_panel_wide">
                        <t t-if="state.purchase_spend_trend.length">
                            <ChartWidget
                                title="purchaseSpendTrendTitle"
                                chartType="'line'"
                                data="purchaseSpendTrendChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onPurchaseSpendTrendChartClick"
                                exportFilename="'aqua_purchase_spend_trend'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title" t-esc="purchaseSpendTrendTitle"/></div><div class="o_aqua_empty">No confirmed purchase orders yet.</div></div></t>
                    </div>
                    <div class="o_aqua_panel_wide">
                        <t t-if="state.avg_price_per_kg_trend.length">
                            <ChartWidget
                                title="avgPriceTrendTitle"
                                chartType="'line'"
                                data="avgPriceTrendChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onAvgPriceTrendChartClick"
                                exportFilename="'aqua_avg_price_trend'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title" t-esc="avgPriceTrendTitle"/></div><div class="o_aqua_empty">Not enough data yet for a trend line.</div></div></t>
                    </div>
                </div>

                <div class="o_aqua_chart_row">
                    <div class="o_aqua_panel_narrow">
                        <t t-if="state.receipt_status_breakdown.length">
                            <ChartWidget
                                title="'Receipt Status Breakdown'"
                                chartType="'doughnut'"
                                data="receiptStatusChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onReceiptStatusChartClick"
                                exportFilename="'aqua_receipt_status'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Receipt Status Breakdown</div></div><div class="o_aqua_empty">No catch receipts recorded yet.</div></div></t>
                    </div>
                    <div class="o_aqua_panel_wide">
                        <t t-if="state.spend_by_vendor.length">
                            <ChartWidget
                                title="'Purchase Spend by Vendor (₹)'"
                                chartType="'horizontalBar'"
                                data="spendByVendorChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onSpendByVendorChartClick"
                                exportFilename="'aqua_spend_by_vendor'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Purchase Spend by Vendor (₹)</div></div><div class="o_aqua_empty">No confirmed purchase orders yet.</div></div></t>
                    </div>
                </div>

                <div class="o_aqua_chart_row">
                    <div class="o_aqua_panel_wide">
                        <t t-if="state.weight_by_vendor.length">
                            <ChartWidget
                                title="'Received Weight by Vendor (kg)'"
                                chartType="'horizontalBar'"
                                data="weightByVendorChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onWeightByVendorChartClick"
                                exportFilename="'aqua_weight_by_vendor'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Received Weight by Vendor (kg)</div></div><div class="o_aqua_empty">No catch receipts recorded yet.</div></div></t>
                    </div>
                    <div class="o_aqua_panel_wide">
                        <t t-if="state.ordered_vs_received.length">
                            <ChartWidget
                                title="'Ordered vs Received Weight (kg)'"
                                chartType="'bar'"
                                data="orderedVsReceivedChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onOrderedVsReceivedChartClick"
                                exportFilename="'aqua_ordered_vs_received'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Ordered vs Received Weight (kg)</div></div><div class="o_aqua_empty">No PO-linked catch receipts yet.</div></div></t>
                    </div>
                </div>

                <div class="o_aqua_section_label">Traceability</div>
                <!-- One row per delivery: the actual granularity of "what happened",
                     carrying the purchase context (Vendor/PO/Species/Ordered) alongside the
                     delivery detail (Batch/Lot/Backorder/Qty/Status), instead of two separate
                     tables that both keyed on Receipt and repeated most of the same story. -->
                <div class="o_aqua_panel_full">
                    <div class="aqua-chart-card">
                        <div class="aqua-chart-card__header">
                            <div class="aqua-chart-card__title">Catch Receipts</div>
                        </div>
                        <t t-if="state.recent_receipts_table.length">
                            <div class="o_aqua_table_scroll">
                            <table class="o_aqua_table">
                                <thead>
                                    <tr>
                                        <th>Receipt</th>
                                        <th>Vendor</th>
                                        <th>PO</th>
                                        <th>Species</th>
                                        <th>Ordered (kg)</th>
                                        <th>Transfer</th>
                                        <th>Batch</th>
                                        <th>Lot/Serial</th>
                                        <th>Backorder Of</th>
                                        <th>Qty Received (kg)</th>
                                        <th>Receipt Status</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <t t-foreach="state.recent_receipts_table" t-as="row" t-key="row.id">
                                        <tr class="o_aqua_table_row_clickable" t-on-click="() => this.onRecentDeliveryRowClick(row.picking_id)">
                                            <td>
                                                <a href="#" t-on-click.stop.prevent="() => this.onRecentReceiptRowClick(row.receipt_id)" t-esc="row.receipt"/>
                                            </td>
                                            <td t-esc="row.vendor"/>
                                            <td t-esc="row.po"/>
                                            <td t-esc="row.species"/>
                                            <td t-esc="row.ordered_qty"/>
                                            <td t-esc="row.transfer"/>
                                            <td t-esc="row.batch"/>
                                            <td t-esc="row.lot_number"/>
                                            <td t-esc="row.backorder_of"/>
                                            <td t-esc="row.quantity"/>
                                            <td><span t-att-class="'aqua-badge ' + badgeClass(row.receipt_state)" t-esc="row.receipt_state"/></td>
                                        </tr>
                                    </t>
                                </tbody>
                            </table>
                            </div>
                        </t>
                        <t t-else="">
                            <div class="o_aqua_empty">No catch receipts recorded yet.</div>
                        </t>
                    </div>
                </div>
            </t>


            <!-- ══════════════════════ PROCESSING TAB ══════════════════════ -->
            <!-- Covers the full intake -> cold storage flow: Catch Receipt ->
                 Processing Order (input qty) -> Blast Freeze
                 -> Cold Room storage. -->
            <t t-if="isTabActive('processing')">
                <div class="o_aqua_kpi_row">
                    <KpiTile
                        label="'Total Processing Orders'"
                        value="state.total_processing_orders"
                        valueFmt="'number'"
                        icon="'fa-industry'"
                        color="'#2C7A7B'"
                        isLoading="state.isLoading"
                        changePct="state.comparison_pct.total_processing_orders"
                        compareLabel="state.comparison_label"
                        onDrill.bind="onDrillTotalProcessingOrders"/>
                    <KpiTile
                        label="'Total Input (kg)'"
                        value="state.total_input_qty"
                        valueFmt="'number'"
                        icon="'fa-balance-scale'"
                        color="'#3182CE'"
                        isLoading="state.isLoading"
                        changePct="state.comparison_pct.total_input_qty"
                        compareLabel="state.comparison_label"
                        onDrill.bind="onDrillTotalProcessingOrders"/>
                    <KpiTile
                        label="'Active Blast Freeze Cycles'"
                        value="state.active_blast_freeze_count"
                        valueFmt="'number'"
                        icon="'fa-snowflake-o'"
                        color="'#38A169'"
                        isLoading="state.isLoading"/>
                    <KpiTile
                        label="'Staged for Processing (kg)'"
                        value="state.wip_stock_kg"
                        valueFmt="'number'"
                        icon="'fa-cube'"
                        color="'#805AD5'"
                        isLoading="state.isLoading"
                        onDrill.bind="onDrillWipStock"/>
                </div>

                <!-- Raw material already pulled from Storage into Pre-/Post-Production for an
                     active Processing Order, but not yet consumed into output. This is the
                     "AQP/Pre-Production" figure Procurement's Live Inventory used to show by
                     mistake; it belongs here, next to the orders actually consuming it. -->
                <div class="o_aqua_chart_row">
                    <div class="o_aqua_panel_full">
                        <t t-if="state.wip_stock_by_product.length">
                            <ChartWidget
                                title="'Staged for Processing (WIP) by Product (kg)'"
                                chartType="'horizontalBar'"
                                data="wipStockByProductChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onWipStockByProductChartClick"
                                exportFilename="'aqua_wip_stock_by_product'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Staged for Processing (WIP) by Product (kg)</div></div><div class="o_aqua_empty">Nothing currently staged for processing.</div></div></t>
                    </div>
                </div>

                <div class="o_aqua_chart_row">
                    <div class="o_aqua_panel_full">
                        <t t-if="state.processing_status_breakdown.length">
                            <ChartWidget
                                title="'Processing Order Status'"
                                chartType="'doughnut'"
                                data="processingStatusChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onProcessingStatusChartClick"
                                exportFilename="'aqua_processing_status'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Processing Order Status</div></div><div class="o_aqua_empty">No processing orders recorded yet.</div></div></t>
                    </div>
                </div>

                <div class="o_aqua_chart_row">
                    <div class="o_aqua_panel_full">
                        <t t-if="state.input_qty_by_species.length">
                            <ChartWidget
                                title="'Input Weight by Species (kg)'"
                                chartType="'horizontalBar'"
                                data="inputQtyBySpeciesChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onInputQtyBySpeciesChartClick"
                                exportFilename="'aqua_input_qty_by_species'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Input Weight by Species (kg)</div></div><div class="o_aqua_empty">No processing orders recorded yet.</div></div></t>
                    </div>
                </div>

                <div class="o_aqua_chart_row">
                    <div class="o_aqua_panel_wide">
                        <t t-if="state.cold_room_utilization.length">
                            <ChartWidget
                                title="'Cold Room Utilization'"
                                chartType="'horizontalBar'"
                                data="coldRoomChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onColdRoomChartClick"
                                exportFilename="'aqua_cold_room_utilization'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Cold Room Utilization</div></div><div class="o_aqua_empty">No cold rooms configured yet.</div></div></t>
                    </div>
                    <div class="o_aqua_panel_narrow">
                        <t t-if="state.blast_freeze_status.length">
                            <ChartWidget
                                title="'Blast Freeze Cycle Status'"
                                chartType="'doughnut'"
                                data="blastFreezeChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onBlastFreezeChartClick"
                                exportFilename="'aqua_blast_freeze_status'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Blast Freeze Cycle Status</div></div><div class="o_aqua_empty">No blast freeze cycles recorded yet.</div></div></t>
                    </div>
                </div>

                <div class="o_aqua_panel_full">
                    <div class="aqua-chart-card">
                        <div class="aqua-chart-card__header">
                            <div class="aqua-chart-card__title">Cold Room Detail</div>
                        </div>
                        <t t-if="state.cold_room_utilization.length">
                            <div class="o_aqua_table_scroll">
                            <table class="o_aqua_table">
                                <thead><tr><th>Room</th><th>Capacity (kg)</th><th>Active Lots</th><th>Utilization %</th></tr></thead>
                                <tbody>
                                    <t t-foreach="state.cold_room_utilization" t-as="room" t-key="room.name">
                                        <tr class="o_aqua_table_row_clickable" t-on-click="() => this.onColdRoomChartClick({label: room.name})">
                                            <td t-esc="room.name"/>
                                            <td t-esc="room.capacity_kg"/>
                                            <td t-esc="room.lots"/>
                                            <td t-esc="room.pct"/>
                                        </tr>
                                    </t>
                                </tbody>
                            </table>
                            </div>
                        </t>
                        <t t-else="">
                            <div class="o_aqua_empty">No cold rooms configured yet.</div>
                        </t>
                    </div>
                </div>

                <div class="o_aqua_panel_full">
                    <div class="aqua-chart-card">
                        <div class="aqua-chart-card__header">
                            <div class="aqua-chart-card__title">Recent Processing Orders</div>
                        </div>
                        <t t-if="state.recent_processing_table.length">
                            <div class="o_aqua_table_scroll">
                            <table class="o_aqua_table">
                                <thead>
                                    <tr>
                                        <th>Order</th>
                                        <th>Species</th>
                                        <th>Catch Receipt</th>
                                        <th>Input (kg)</th>
                                        <th>IPQC</th>
                                        <th>Status</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <t t-foreach="state.recent_processing_table" t-as="row" t-key="row.id">
                                        <tr class="o_aqua_table_row_clickable" t-on-click="() => this.onRecentProcessingRowClick(row.id)">
                                            <td t-esc="row.name"/>
                                            <td t-esc="row.species"/>
                                            <td t-esc="row.catch_receipt"/>
                                            <td t-esc="row.qty_input"/>
                                            <td><span t-att-class="'aqua-badge ' + badgeClass(row.ipqc_status)" t-esc="row.ipqc_status"/></td>
                                            <td><span t-att-class="'aqua-badge ' + badgeClass(row.state)" t-esc="row.state"/></td>
                                        </tr>
                                    </t>
                                </tbody>
                            </table>
                            </div>
                        </t>
                        <t t-else="">
                            <div class="o_aqua_empty">No processing orders recorded yet.</div>
                        </t>
                    </div>
                </div>
            </t>

            <!-- ══════════════════════ QUALITY CONTROL TAB ══════════════════════ -->
            <!-- Two real gates plus a final check before it leaves the plant: IQC (Incoming,
                 at Receiving ; see the Procurement tab for the purchase side of the same
                 gate) -> IPQC (In-Process, on the shop floor during manufacturing, one check
                 per station: Cleaning/Peeling/Deveining/Freezing/Grading/Packing) -> Final QC
                 (Pre-Shipment). Each gets its own numbers below instead of one blended
                 "QC Pass Rate" that can't tell you which stage is actually failing. -->
            <t t-if="isTabActive('quality')">
                <div class="o_aqua_section_label">Overall</div>
                <div class="o_aqua_kpi_row">
                    <KpiTile
                        label="'QC Pass Rate'"
                        value="state.qc_pass_rate"
                        valueFmt="'pct'"
                        icon="'fa-flask'"
                        color="'#38A169'"
                        isLoading="state.isLoading"
                        changePct="state.comparison_pct.qc_pass_rate"
                        compareLabel="state.comparison_label"
                        onDrill.bind="onDrillQcPassRate"/>
                    <KpiTile
                        label="'Total Checks'"
                        value="state.qc_total"
                        valueFmt="'number'"
                        icon="'fa-clipboard'"
                        color="'#2C7A7B'"
                        isLoading="state.isLoading"
                        changePct="state.comparison_pct.qc_total"
                        compareLabel="state.comparison_label"
                        onDrill.bind="onDrillQcTotal"/>
                    <KpiTile
                        label="'Failed Checks'"
                        value="state.qc_fail_count"
                        valueFmt="'number'"
                        icon="'fa-times-circle'"
                        color="'#C53030'"
                        isLoading="state.isLoading"
                        onDrill.bind="onDrillQcFail"/>
                    <KpiTile
                        label="'On Hold'"
                        value="state.qc_hold_count"
                        valueFmt="'number'"
                        icon="'fa-pause-circle'"
                        color="'#D69E2E'"
                        isLoading="state.isLoading"
                        onDrill.bind="onDrillQcHold"/>
                </div>

                <div class="o_aqua_chart_row">
                    <div class="o_aqua_panel_narrow">
                        <t t-if="state.qc_breakdown.length">
                            <ChartWidget
                                title="'QC Result Breakdown'"
                                chartType="'doughnut'"
                                data="qcChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onQcChartClick"
                                exportFilename="'aqua_qc_breakdown'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">QC Result Breakdown</div></div><div class="o_aqua_empty">No QC tests recorded yet.</div></div></t>
                    </div>
                    <div class="o_aqua_panel_narrow">
                        <t t-if="state.qc_stage_breakdown.length">
                            <ChartWidget
                                title="'Checks by Stage — IQC / IPQC / Final'"
                                chartType="'doughnut'"
                                data="qcStageChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onQcStageChartClick"
                                exportFilename="'aqua_qc_stage_breakdown'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Checks by Stage — IQC / IPQC / Final</div></div><div class="o_aqua_empty">No QC tests recorded yet.</div></div></t>
                    </div>
                    <div class="o_aqua_panel_narrow">
                        <t t-if="state.qc_trend.length">
                            <ChartWidget
                                title="qcTrendTitle"
                                chartType="'line'"
                                data="qcTrendChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onQcTrendChartClick"
                                exportFilename="'aqua_qc_trend'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title" t-esc="qcTrendTitle"/></div><div class="o_aqua_empty">Not enough data yet for a trend line.</div></div></t>
                    </div>
                </div>

                <!-- IQC ; gate inspection at the plant on arrival, before raw material moves
                     from Input to Quality Control to Storage (see the Procurement tab). -->
                <div class="o_aqua_section_label">IQC — Incoming (Receiving)</div>
                <div class="o_aqua_kpi_row">
                    <KpiTile
                        label="'IQC Checks'"
                        value="state.iqc_total"
                        valueFmt="'number'"
                        icon="'fa-truck'"
                        color="'#2C7A7B'"
                        isLoading="state.isLoading"
                        onDrill.bind="onDrillIqcTotal"/>
                    <KpiTile
                        label="'IQC Pass Rate'"
                        value="state.iqc_pass_rate"
                        valueFmt="'pct'"
                        icon="'fa-flask'"
                        color="'#38A169'"
                        isLoading="state.isLoading"
                        changePct="state.comparison_pct.iqc_pass_rate"
                        compareLabel="state.comparison_label"
                        onDrill.bind="onDrillIqcTotal"/>
                    <KpiTile
                        label="'Avg Histamine (ppm)'"
                        value="state.avg_histamine_ppm"
                        valueFmt="'number'"
                        icon="'fa-vial'"
                        color="'#805AD5'"
                        isLoading="state.isLoading"/>
                    <KpiTile
                        label="'Avg Sensory Score (0-10)'"
                        value="state.avg_sensory_score"
                        valueFmt="'number'"
                        icon="'fa-star'"
                        color="'#3182CE'"
                        isLoading="state.isLoading"/>
                </div>

                <div class="o_aqua_chart_row">
                    <div class="o_aqua_panel_narrow">
                        <t t-if="state.intake_decision_breakdown.length">
                            <ChartWidget
                                title="'Intake Decision (IQC)'"
                                chartType="'doughnut'"
                                data="intakeDecisionChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onIntakeDecisionChartClick"
                                exportFilename="'aqua_intake_decision'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Intake Decision (IQC)</div></div><div class="o_aqua_empty">No intake decisions recorded yet.</div></div></t>
                    </div>
                    <div class="o_aqua_panel_wide">
                        <t t-if="state.residue_screening.labels.length">
                            <ChartWidget
                                title="'Residue Screening Results (IQC)'"
                                chartType="'bar'"
                                data="residueScreeningChartData"
                                isLoading="state.isLoading"
                                exportFilename="'aqua_residue_screening'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Residue Screening Results (IQC)</div></div><div class="o_aqua_empty">No residue screening data yet.</div></div></t>
                    </div>
                </div>

                <div class="o_aqua_chart_row">
                    <div class="o_aqua_panel_full">
                        <t t-if="state.rejected_qty_by_species.length">
                            <ChartWidget
                                title="'Rejected Quantity by Species (kg, IQC)'"
                                chartType="'horizontalBar'"
                                data="rejectedQtyBySpeciesChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onRejectedQtyBySpeciesChartClick"
                                exportFilename="'aqua_rejected_qty_by_species'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Rejected Quantity by Species (kg, IQC)</div></div><div class="o_aqua_empty">No rejected raw material recorded yet.</div></div></t>
                    </div>
                </div>

                <!-- IPQC ; one check per shop-floor station (Cleaning, Peeling, Deveining,
                     Freezing(IQF), Grading, Packing), raised against the Work Order and
                     linked to its Processing Order (see quality_check.py
                     _aqua_auto_link_in_process for how these get onto this dashboard). -->
                <div class="o_aqua_section_label">IPQC — In-Process (Manufacturing)</div>
                <div class="o_aqua_kpi_row">
                    <KpiTile
                        label="'IPQC Checks'"
                        value="state.ipqc_total"
                        valueFmt="'number'"
                        icon="'fa-industry'"
                        color="'#2C7A7B'"
                        isLoading="state.isLoading"
                        onDrill.bind="onDrillIpqcTotal"/>
                    <KpiTile
                        label="'IPQC Pass Rate'"
                        value="state.ipqc_pass_rate"
                        valueFmt="'pct'"
                        icon="'fa-flask'"
                        color="'#38A169'"
                        isLoading="state.isLoading"
                        changePct="state.comparison_pct.ipqc_pass_rate"
                        compareLabel="state.comparison_label"
                        onDrill.bind="onDrillIpqcTotal"/>
                </div>

                <div class="o_aqua_chart_row">
                    <div class="o_aqua_panel_full">
                        <t t-if="state.ipqc_by_operation_chart.labels.length">
                            <ChartWidget
                                title="'IPQC Pass / Fail by Station'"
                                chartType="'bar'"
                                data="ipqcByOperationChartData"
                                isLoading="state.isLoading"
                                onElementClick.bind="onIpqcByOperationChartClick"
                                exportFilename="'aqua_ipqc_by_operation'"/>
                        </t>
                        <t t-else=""><div class="aqua-chart-card"><div class="aqua-chart-card__header"><div class="aqua-chart-card__title">IPQC Pass / Fail by Station</div></div><div class="o_aqua_empty">No in-process checks recorded yet.</div></div></t>
                    </div>
                </div>

                <!-- Final QC ; last check before a Pack Order ships. -->
                <div class="o_aqua_section_label">Final QC (Pre-Shipment)</div>
                <div class="o_aqua_kpi_row">
                    <KpiTile
                        label="'Final QC Checks'"
                        value="state.final_qc_total"
                        valueFmt="'number'"
                        icon="'fa-ship'"
                        color="'#2C7A7B'"
                        isLoading="state.isLoading"
                        onDrill.bind="onDrillFinalQcTotal"/>
                    <KpiTile
                        label="'Final QC Pass Rate'"
                        value="state.final_qc_pass_rate"
                        valueFmt="'pct'"
                        icon="'fa-flask'"
                        color="'#38A169'"
                        isLoading="state.isLoading"
                        onDrill.bind="onDrillFinalQcTotal"/>
                </div>

                <div class="o_aqua_section_label">Traceability</div>
                <div class="o_aqua_panel_full">
                    <div class="aqua-chart-card">
                        <div class="aqua-chart-card__header">
                            <div class="aqua-chart-card__title">Recent Quality Checks</div>
                        </div>
                        <t t-if="state.recent_qc_table.length">
                            <div class="o_aqua_table_scroll">
                            <table class="o_aqua_table">
                                <thead>
                                    <tr>
                                        <th>Check</th>
                                        <th>Stage</th>
                                        <th>Source</th>
                                        <th>Operation</th>
                                        <th>Result</th>
                                        <th>Decision</th>
                                        <th>Histamine (ppm)</th>
                                        <th>Core Temp (°C)</th>
                                        <th>On Hold</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <t t-foreach="state.recent_qc_table" t-as="row" t-key="row.id">
                                        <tr class="o_aqua_table_row_clickable" t-on-click="() => this.onRecentQcRowClick(row.id)">
                                            <td t-esc="row.name"/>
                                            <td><span class="aqua-badge aqua-badge--purple" t-esc="row.stage"/></td>
                                            <td t-esc="row.source"/>
                                            <td t-esc="row.operation"/>
                                            <td><span t-att-class="'aqua-badge ' + badgeClass(row.result)" t-esc="row.result"/></td>
                                            <td t-esc="row.decision"/>
                                            <td t-esc="row.histamine_ppm"/>
                                            <td t-esc="row.core_temp_c"/>
                                            <td><span t-att-class="'aqua-badge ' + (row.on_hold ? 'aqua-badge--amber' : 'aqua-badge--gray')" t-esc="row.on_hold ? 'Yes' : 'No'"/></td>
                                        </tr>
                                    </t>
                                </tbody>
                            </table>
                            </div>
                        </t>
                        <t t-else="">
                            <div class="o_aqua_empty">No quality checks recorded yet.</div>
                        </t>
                    </div>
                </div>
            </t>

            <!-- ══════════════════════ BUDGET TAB ══════════════════════ -->
            <!-- Reads the currently-active Budget (Accounting > Budgets) for this
                 company, the same record configured under Analytic Accounts /
                 Budgetary Positions / Budgets. This tab is a read-only summary;
                 all editing still happens in Accounting. -->
            <t t-if="isTabActive('budget')">
                <div class="o_aqua_section_label">
                    <t t-if="state.budget_name" t-esc="state.budget_name"/>
                    <t t-else="">No Active Budget</t>
                    <t t-if="state.budget_period_from">
                        (<t t-esc="state.budget_period_from"/> – <t t-esc="state.budget_period_to"/>)
                    </t>
                </div>

                <t t-if="!state.budget_id">
                    <div class="o_aqua_panel_full">
                        <div class="o_aqua_empty">No confirmed budget found for this company. Set one up under Accounting → Budgets.</div>
                    </div>
                </t>

                <t t-if="state.budget_id">
                    <div class="o_aqua_kpi_row">
                        <KpiTile
                            label="'Total Planned'"
                            value="state.budget_total_planned"
                            valueFmt="'number'"
                            icon="'fa-bullseye'"
                            color="'#3182CE'"
                            isLoading="state.isLoading"
                            onDrill.bind="onOpenBudgetRecord"/>
                        <KpiTile
                            label="'Total Practical (Actual)'"
                            value="state.budget_total_practical"
                            valueFmt="'number'"
                            icon="'fa-inr'"
                            color="'#38A169'"
                            isLoading="state.isLoading"
                            onDrill.bind="onOpenBudgetRecord"/>
                        <KpiTile
                            label="'Total Theoretical (Pace)'"
                            value="state.budget_total_theoretical"
                            valueFmt="'number'"
                            icon="'fa-clock-o'"
                            color="'#D69E2E'"
                            isLoading="state.isLoading"
                            onDrill.bind="onOpenBudgetRecord"/>
                        <KpiTile
                            label="'Achievement'"
                            value="state.budget_achievement_pct"
                            valueFmt="'pct'"
                            icon="'fa-line-chart'"
                            color="'#805AD5'"
                            isLoading="state.isLoading"
                            onDrill.bind="onOpenBudgetRecord"/>
                        <KpiTile
                            label="'Gross Margin'"
                            value="state.budget_total_gross_margin"
                            valueFmt="'number'"
                            icon="'fa-inr'"
                            color="'#DD6B20'"
                            isLoading="state.isLoading"
                            onDrill.bind="onOpenBudgetRecord"/>
                    </div>

                    <div class="o_aqua_chart_row">
                        <div class="o_aqua_panel_full">
                            <t t-if="state.budget_chart.length">
                                <ChartWidget
                                    title="'Planned vs Practical by Cost Center'"
                                    chartType="'bar'"
                                    data="budgetChartData"
                                    isLoading="state.isLoading"
                                    exportFilename="'aqua_budget_chart'"/>
                            </t>
                            <t t-else="">
                                <div class="aqua-chart-card">
                                    <div class="aqua-chart-card__header"><div class="aqua-chart-card__title">Planned vs Practical by Cost Center</div></div>
                                    <div class="o_aqua_empty">No budget lines added yet.</div>
                                </div>
                            </t>
                        </div>
                    </div>

                    <div class="o_aqua_section_label">Budget Lines</div>
                    <div class="o_aqua_panel_full">
                        <div class="aqua-chart-card">
                            <div class="aqua-chart-card__header">
                                <div class="aqua-chart-card__title">By Cost Center</div>
                                <button type="button" class="btn btn-sm btn-outline-secondary" t-on-click="onOpenBudgetRecord">
                                    Open in Accounting
                                </button>
                            </div>
                            <t t-if="state.budget_lines_table.length">
                                <div class="o_aqua_table_scroll">
                                <table class="o_aqua_table">
                                    <thead>
                                        <tr>
                                            <th>Budgetary Position</th>
                                            <th>Analytic Account</th>
                                            <th>Planned</th>
                                            <th>Practical</th>
                                            <th>Theoretical</th>
                                            <th>Achievement</th>
                                            <th>Gross Margin</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <t t-foreach="state.budget_lines_table" t-as="row" t-key="row_index">
                                            <tr class="o_aqua_table_row_clickable" t-on-click="onOpenBudgetRecord">
                                                <td t-esc="row.position"/>
                                                <td t-esc="row.analytic_account"/>
                                                <td t-esc="row.planned"/>
                                                <td t-esc="row.practical"/>
                                                <td t-esc="row.theoretical"/>
                                                <td><t t-esc="row.achievement"/>%</td>
                                                <td t-esc="row.gross_margin"/>
                                            </tr>
                                        </t>
                                    </tbody>
                                </table>
                                </div>
                            </t>
                            <t t-else="">
                                <div class="o_aqua_empty">No budget lines added yet.</div>
                            </t>
                        </div>
                    </div>
                </t>
            </t>

            <DrillPanel
                isOpen="state.drill.isOpen"
                title="state.drill.title"
                model="state.drill.model"
                records="state.drill.records"
                columns="state.drill.columns"
                isLoading="state.drill.loading"
                onClose.bind="closeDrill"/>

        </div>
    </t>
</templates>